# 二期接口实现检查清单

适用范围：
- 用于二期接口开发、联调、提交前的实现自检
- 当前只覆盖后台 `admin` 场景

当前状态：
- 本文档不是接口设计说明，而是“实现是否落全”的检查口径
- 适合后端开发、联调负责人、评审时共同使用

关联文档：
- `二期接口与现有 custom_addons 文件落位映射表.md`
- `二期 controller/service/model 具体文件创建建议.md`
- `二期接口入参校验与错误码清单.md`
- `标准订单导入模板字段说明与错误反馈规范.md`

---

## 1. 开工前检查

- [ ] 已确认本次只做二期相较一期的升级补充，不重写一期稳定接口
- [ ] 已确认接口范围属于 `admin` 场景，路径前缀统一为 `/api/admin/logistics/...`
- [ ] 已确认首页、统计、导入接口出口放 `logistics_web/controllers/`
- [ ] 已确认运单详情三层读取与导入业务落在 `logistics_dispatch/models/`
- [ ] 已确认 `trace / evidence / exception` 继续只做领域 helper，不新增独立 API controller
- [ ] 已阅读 `二期接口差异清单.md`，知道哪些是一期复用、哪些是二期新增、哪些是扩字段

---

## 2. 文件落位检查

- [ ] `custom_addons/logistics_web/controllers/` 下已补首页 controller
- [ ] `custom_addons/logistics_web/controllers/` 下已补统计 controller
- [ ] `custom_addons/logistics_web/controllers/` 下已补导入 controller
- [ ] `custom_addons/logistics_web/services/` 目录已创建
- [ ] `custom_addons/logistics_web/services/` 下已补 `home_service.py`
- [ ] `custom_addons/logistics_web/services/` 下已补 `stats_service.py`
- [ ] `custom_addons/logistics_web/services/` 下已补 `import_service.py`
- [ ] `custom_addons/logistics_web/__init__.py` 已补 `from . import services`
- [ ] `custom_addons/logistics_web/controllers/__init__.py` 已补新 controller import
- [ ] `custom_addons/logistics_dispatch/models/` 下已补 `logistics_dispatch_waybill_api.py`
- [ ] `custom_addons/logistics_dispatch/models/` 下已补 `logistics_dispatch_waybill_customer_line_api.py`
- [ ] `custom_addons/logistics_dispatch/models/` 下已补 `logistics_dispatch_waybill_customer_goods_line_api.py`
- [ ] `custom_addons/logistics_dispatch/models/` 下已补 `logistics_import_batch.py`
- [ ] `custom_addons/logistics_dispatch/models/__init__.py` 已补上述新模型扩展 import
- [ ] 未把新方法误写进未加载的历史文件 `logistics_dispatch_waybill_customer_line.py`
- [ ] 未把新方法误写进未加载的历史文件 `logistics_dispatch_waybill_customer_goods_line.py`

---

## 3. 路由与接口检查

### 首页域

- [ ] 已实现 `getHomeOverviewSummary`
- [ ] 已实现 `getHomeRecentActivities`
- [ ] 已实现 `getHomeGuideByRole`
- [ ] controller 只做入参解析、权限检查、service 调用、统一响应封装

### 统计域

- [ ] 已实现 `getStatsOverviewByDateRange`
- [ ] 已实现 `getStatsTrendByMetric`
- [ ] 已实现 `getStatsRankingByDimension`
- [ ] 已限制可用指标白名单、维度白名单、时间粒度白名单

### 运单详情三层读取

- [ ] 已实现 `getWaybillDetailV2`
- [ ] 已实现 `getWaybillCustomerLinesByWaybill`
- [ ] 已实现 `getWaybillCustomerGoodsLinesByWaybill`
- [ ] `getWaybillDetail` 兼容扩字段策略已明确，不会直接破坏一期前端

### 导入链路

- [ ] 已实现 `downloadWaybillStandardTemplate`
- [ ] 已实现 `precheckWaybillStandardImport`
- [ ] 已实现 `confirmWaybillStandardImport`
- [ ] 已实现 `getWaybillImportResult`
- [ ] 模板下载逻辑与现有 `logistics_dispatch/static/src/import_templates/` 保持一致

---

## 4. 入参与错误码检查

- [ ] 每个接口都已按《二期接口入参校验与错误码清单》实现校验
- [ ] 每个接口都已返回统一错误结构
- [ ] 未把业务错误直接裸抛为 Python 原始异常栈
- [ ] 时间范围、排序字段、分页参数、指标名都已做白名单或边界校验
- [ ] 导入文件大小、扩展名、模板版本已校验
- [ ] 预校验 token 的失效规则已实现
- [ ] 正式导入前会验证 `precheck_token`、模板版本、文件摘要或批次一致性

---

## 5. 模型与数据检查

- [ ] 运单详情聚合字段读取基于 `logistics.dispatch.waybill`
- [ ] 客户明细读取基于 `logistics.dispatch.waybill.customer.line`
- [ ] 货物明细读取基于 `logistics.dispatch.waybill.customer.goods.line`
- [ ] 首页异常聚合基于 `logistics.trace.exception`
- [ ] 首页证据聚合基于 `logistics.trace.evidence`
- [ ] 最新留痕、留痕摘要等基础数据复用 `logistics_trace_core` 现有扩展
- [ ] 如新增 `logistics.import.batch` 模型，已补充必要字段、索引与唯一约束
- [ ] 如新增 `logistics.import.batch` 模型，已补 `security/ir.model.access.csv`

---

## 6. 导入链路专项检查

- [ ] 模板字段、字段顺序、字段中文名与《标准订单导入模板字段说明与错误反馈规范》一致
- [ ] 预校验会返回文件级、行级、字段级错误
- [ ] 客户明细与货物明细的分层归组规则已实现
- [ ] 运单号、客户号、客户名称、门店号、门店名称等业务键解析规则已实现
- [ ] 重复行、缺失行、跨层不一致、主键找不到等错误都有明确错误码
- [ ] 正式导入不会绕过预校验直接落库
- [ ] 导入结果页可返回成功数、失败数、运单数、客户明细数、货物明细数

---

## 7. Odoo 模块装配检查

- [ ] 新增 Python 文件后，对应 `__init__.py` 均已导入
- [ ] 如有新增模型，模块升级时能被正确加载
- [ ] 如需要访问新模型，`security/ir.model.access.csv` 已补
- [ ] 如引入新的数据文件或视图文件，相关 `__manifest__.py` 已补 `data`
- [ ] 纯 Python `services/` 目录无需写入 `__manifest__.py`，但必须被 `__init__.py` 导入

---

## 8. 联调检查

- [ ] 已按《二期接口实现优先级与联调顺序表》安排联调顺序
- [ ] 首页域已与前端首页卡片、趋势卡片、角色引导联调
- [ ] 统计域已与所有统计图表页联调
- [ ] 运单详情三层读取已与详情页新结构联调
- [ ] 导入链路已完成“模板下载 -> 预校验 -> 正式导入 -> 结果页”闭环联调
- [ ] 至少准备 1 份成功样例、1 份字段错误样例、1 份跨层关联错误样例

---

## 9. 提交前收口检查

- [ ] 没有把首页、统计、导入业务逻辑继续堆进单一 `logistics_web_dashboard.py`
- [ ] 没有把聚合流程写死在 controller 中
- [ ] 没有直接修改官方 Odoo addon
- [ ] 没有破坏一期已有稳定接口返回结构
- [ ] 已同步更新 `ai-code/docs/change_notes/`
- [ ] 相关实现说明已能回链到本目录接口文档

---

## 10. 验收口径

满足以下 4 条时，可认为“已经具备按仓库现状开工并进入联调”的条件：

1. 新接口都有明确 controller / service / model 落位
2. 三层运单详情与导入批次模型都有实际承接点
3. 校验规则、错误码、模板规则已经在实现中对齐
4. 模块装配、自检、联调路径已经闭环
