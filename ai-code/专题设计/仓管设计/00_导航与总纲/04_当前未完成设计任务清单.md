# 当前未完成设计任务清单

## 版本信息

- 文档名称：当前未完成设计任务清单
- 当前版本：`v0.1`
- 当前阶段：总导航层待办跟踪版
- 所属专题：仓管模块 / 导航与总纲
- 当前用途：
  - 汇总当前仍未完全收口的设计任务
  - 作为后续持续跟踪与续写时的待办入口
  - 作为“下一步该继续补什么”的总清单

## 目录索引

- `1. 文档定位`
- `2. 当前未完成设计任务总览`
- `3. 仍需继续收口的总导航层任务`
- `4. 仍需继续深化的跨模块规范任务`
- `5. 仍需继续深化的数据库与图稿任务`
- `6. 仍需继续深化的页面、交互与组件任务`
- `7. 仍需继续深化的实现映射与总校对任务`
- `8. 推荐处理顺序`
- `9. 当前建议联读专题`
- `10. v0.1 收口与后续补充方向`

## 章节总览

这份清单当前主要回答 3 个问题：

1. 还有哪些设计任务没有完全做完
2. 这些未完成项分别属于哪条主线
3. 下次继续写时应该优先补哪一批

建议阅读方式：

1. 先看 `2`，快速知道当前还剩哪些类型的工作
2. 再看 `3` 到 `7`，定位到对应主线的未完成项
3. 最后看 `8` 到 `10`，明确优先顺序和续写入口

## 1. 文档定位

本文件不是替代各专题正文，也不是替代进度总结。

它的作用是：

- 把当前“还没完全收口”的工作单独提出来
- 作为后续持续跟踪页
- 方便下次直接从待办项进入，而不是重新翻整套文档

## 2. 当前未完成设计任务总览

截至当前，仓管设计已经形成了较完整的 `v0.1` 文档体系。  
当前未完成项，更多不是“缺少核心专题”，而是下面三类工作：

- 继续把现有专题往更细的交叉映射、回填、验收状态推进
- 把部分仍是“草稿”口径的专题，逐步升级成更正式的“规范 v0.1”
- 继续补总导航层的总索引、双向回链和跨专题总校对

一句话收口：

`当前最大的未完成项，已经不是“缺主题”，而是“深化现有专题并完成总导航层持续收口”。`

## 3. 仍需继续收口的总导航层任务

当前总导航层仍值得继续推进的方向包括：

- 继续深化 `03_专题_模块_实现对象总关系表.md`
  - 继续补更长链路的跨编号联动总表
  - 继续补方法 / 组件 / 模板对象层的状态、验收、发布、归档、下轮启动链路
- 继续补 `README.md` 与总导航层的更细双向回链
- 继续补各专题末尾的 `当前建议联读专题`
- 必要时再补一份：
  - `专题 -> 模块 -> 实现对象 -> 状态 / 验收 / 联调 / 发布` 的一页式压缩总表

## 4. 仍需继续深化的跨模块规范任务

当前这几份跨模块规范虽然已经很深，但仍可继续推进：

### 4.1 对象与编码统一规范

- 继续补更细的对象编号跨编号链路
- 继续补对象编号到更细实现对象、验收、发布、归档、下轮启动的联动
- 继续考虑是否从“草稿”升级为更正式的“规范 v0.1”

### 4.2 状态与流程统一规范

- 继续补 `ST-* / EST-*` 到更多真实实现对象、验收对象、联调编号的映射
- 继续补与对象稿、数据库稿、日志稿之间的逐编号回链
- 后续可考虑把异常补充状态体系单独抽出附录或更正式子章

### 4.3 日志与操作记录规范

- 继续补日志对象与真实模块文件、方法、页面对象的长期跟踪链路
- 继续补与联调、上线、归档、下轮启动之间的编号链
- 后续可考虑与证据专题形成更细的联读和边界校对

## 5. 仍需继续深化的数据库与图稿任务

数据库线当前已非常完整，但仍可继续推进：

- 数据库稿与状态稿、对象稿、日志稿之间的更细逐编号回链
- 数据库稿与图稿的更细双向编号索引
- 更细的最终落地回填清单
- 如果后续要进入真正实施，可考虑：
  - 把部分“草稿”改成“规范 v0.1（最终校正版）”
  - 把图稿进一步转成正式图库输出版本

## 6. 仍需继续深化的页面、交互与组件任务

这条线当前基线已经稳，但仍有可继续补的点：

- 执行页组件命名专题与状态稿、权限稿之间的更细组件级回填
- `CMP-EST-*` 等异常反馈组件的真实实现状态跟踪
- 页面结构规范、执行页交互规范中的联读专题补齐
- 如后续进入实现前评审，可再补一版：
  - 页面 -> 组件 -> 状态 -> 权限 -> 验收 的一页式关系表

## 7. 仍需继续深化的实现映射与总校对任务

当前最适合持续推进的“总控类任务”包括：

- 各专题末尾 `当前建议联读专题` 的统一补齐
- 专题之间更细的双向回链
- 对象稿、状态稿、日志稿、数据库稿、总关系表之间的交叉校对
- 若后续要进入实现阶段，建议再补：
  - “专题 -> 开发任务 -> 文件 -> 方法 / 组件 -> 验收状态”的压缩总表

## 8. 推荐处理顺序

如果下次继续写，我建议优先顺序如下：

1. 继续深化 `03_专题_模块_实现对象总关系表.md`
2. 继续深化 `对象与编码统一规范草稿.md`
3. 继续深化 `状态与流程统一规范草稿.md`
4. 继续深化 `日志与操作记录规范草稿.md`
5. 再回头统一做一轮：
   - 联读专题补齐
   - 总导航双向回链补齐
   - 跨专题编号校对

## 9. 当前建议联读专题

- `00_导航与总纲/00_当前已拆专题文件总览与v0.1基线清单.md`
- `00_导航与总纲/01_当前设计工作进度总结.md`
- `00_导航与总纲/02_v0.1专题总一致性校对结果.md`
- `00_导航与总纲/03_专题_模块_实现对象总关系表.md`
- `02_跨模块规范/01_对象与编码/00_对象与编码统一规范草稿.md`
- `02_跨模块规范/02_状态与流程/00_状态与流程统一规范草稿.md`
- `02_跨模块规范/04_接口与数据/01_数据库设计规范草稿.md`
- `02_跨模块规范/04_接口与数据/03_日志与操作记录规范草稿.md`

## 10. v0.1 收口与后续补充方向

当前这份未完成设计任务清单先收住了：

- 总导航层仍待继续推进的任务
- 跨模块规范仍待继续推进的任务
- 数据库与图稿仍待继续推进的任务
- 页面、交互、组件仍待继续推进的任务
- 总校对与实现映射仍待继续推进的任务

后续更适合继续补的方向：

- 将未完成项逐步转成带编号的持续跟踪表
- 将高优先级未完成项继续下钻成“任务 -> 文件 -> 方法 / 组件 -> 验收结论”
- 当某一批未完成项收口后，再从这份清单中移出，转入已完成进度总结
