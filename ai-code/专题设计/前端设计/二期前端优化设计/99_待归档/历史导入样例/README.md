# 历史导入样例

本目录用于归档已经退出当前主阅读链的旧导入样例材料。

当前已归档内容：

- 旧的单表 `CSV` 运单样例
- 旧的单表 `CSV` 使用说明

归档原因：

- 当前二期已切换为 `TSL-IMPORT-WAYBILL-V2` 三 Sheet `XLSX` 主口径
- 为避免样例目录继续混用 `CSV` 与 `XLSX` 两套主路径，需要将旧样例移出正式样例区

说明：

- 归档不代表历史资料无效
- 如需回看旧阶段的验证路径，可在本目录中查阅
