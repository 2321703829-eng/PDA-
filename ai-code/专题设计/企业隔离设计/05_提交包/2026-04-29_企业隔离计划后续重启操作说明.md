# 2026-04-29 企业隔离计划后续重启操作说明

## 1. 文档定位

- 本文用于说明：当当前主线需求处理完毕后，如何把本轮暂停的企业隔离计划重新拉起。
- 本文面向当前已经完成过一轮企业隔离 `P0 / P1` 设计、编码、本地验证和文档沉淀之后的恢复场景。
- 本文当前对应的暂停备份锚点为：
  - `ai-code/.safety_backups/enterprise_isolation_pause_2026-04-29_154001`

## 2. 当前已保留的恢复资产

### 2.1 代码与文档快照

当前已经保留的快照目录为：

- `ai-code/.safety_backups/enterprise_isolation_pause_2026-04-29_154001/snapshot`

其中已包含：

- `custom_addons/logistics_trace_core`
- `custom_addons/logistics_trace_evidence`
- `custom_addons/logistics_trace_exception`
- `custom_addons/logistics_web`
- `ai-code/专题设计/企业隔离设计`
- `ai-code/docs/change_notes`
- `ai-code/.runtime`
- `odoo_local.conf`

### 2.2 当前版本状态记录

当前还保留了暂停时的状态材料：

- `branch.txt`
- `head.txt`
- `origin_feat_text_head.txt`
- `git_status_short.txt`
- `git_status_full.txt`
- `tracked_changes.patch`

这些文件可用于确认：

- 暂停时所在分支
- 暂停时提交号
- 暂停时工作区状态
- 暂停时已跟踪文件差异

### 2.3 数据库备份

当前已保留数据库导出：

- `db_dumps/odoo_logistics_dev.dump`
- `db_dumps/tenant_kebi.dump`

这两份备份用于：

- 恢复当时的主开发库状态
- 恢复企业隔离验证租户 `tenant_kebi`

## 3. 后续重启时的建议原则

### 3.1 不直接在当前主分支硬恢复

不建议后续直接在当前需求分支上把企业隔离文件覆盖回去。

建议做法：

- 新开一个专门的恢复分支
- 在恢复分支里重新接回企业隔离相关代码、文档和本地验证配置

### 3.2 先恢复代码与文档，再恢复数据库

建议顺序为：

1. 新开恢复分支
2. 恢复企业隔离代码与文档
3. 恢复本地验证配置
4. 恢复数据库
5. 重新启动本地验证实例
6. 再继续 `P0 / P1` 主线

### 3.3 以暂停快照为单一锚点

后续所有恢复动作，建议统一以：

- `ai-code/.safety_backups/enterprise_isolation_pause_2026-04-29_154001`

作为唯一恢复锚点，避免混用其他零散文件、旧分支或记忆中的状态。

## 4. 建议恢复步骤

### 4.1 第一步：新开恢复分支

建议新开一个专门分支，例如：

```text
feat-enterprise-isolation-restart
```

目标：

- 不污染当前主线需求分支
- 把企业隔离恢复工作与当前新需求隔离开

### 4.2 第二步：恢复企业隔离代码与文档

优先建议从：

- `snapshot`

中恢复，而不是只依赖 `tracked_changes.patch`。

原因：

- 本轮企业隔离不只有已跟踪文件改动
- 还有大量新增文件、运行配置、说明文档和本地验证脚本
- 只打补丁容易漏掉新增文件

建议重点恢复目录：

- `custom_addons/logistics_trace_core`
- `custom_addons/logistics_trace_evidence`
- `custom_addons/logistics_trace_exception`
- `custom_addons/logistics_web`
- `ai-code/专题设计/企业隔离设计`
- `ai-code/docs/change_notes`
- `ai-code/.runtime`

### 4.3 第三步：恢复数据库

如后续还要继续基于当时的运行态推进，建议恢复：

- `odoo_logistics_dev.dump`
- `tenant_kebi.dump`

恢复目标：

- 主开发库回到暂停时的业务状态
- `tenant_kebi` 回到当时的 `P0` 验证态

### 4.4 第四步：恢复本地访问入口

后续恢复时，建议重新确认并拉起：

- `8070`：`tenant_kebi`
- `8071`：`odoo_logistics_dev`
- `8090`：本地租户反向代理验证链

必要时还要重新确认：

- `hosts` 文件中的本地测试域名映射
- 本地浏览器代理绕过配置

### 4.5 第五步：按专题文档恢复推进顺序

恢复后建议不要重新发散设计，而是直接按当时已经收口的顺序继续：

1. 回看 `P1` 定版与交付说明
2. 回看 `P0` 正式部署与正式验收剩余工作清单
3. 根据当前阶段决定先收 `P1` 还是继续推进 `P0`

当前最重要的总入口文档包括：

- `专题设计/企业隔离设计/00_导航与总纲/2026-04-28_企业隔离P0P1总设计文档.md`
- `专题设计/企业隔离设计/00_导航与总纲/2026-04-28_企业隔离P0P1详细总工作计划.md`
- `专题设计/企业隔离设计/05_提交包/2026-04-28_企业隔离P0P1本轮最终交付说明.md`
- `专题设计/企业隔离设计/05_提交包/2026-04-29_企业隔离P1本轮定版说明.md`

## 5. 建议恢复后的优先级判断

### 5.1 如果当前主线需求只是临时插入

建议恢复后优先继续：

- `P0` 正式部署与正式验收

原因：

- `P1` 主体已完成并定版
- 之前真正剩下的主线已转到 `P0`

### 5.2 如果恢复时业务口径已经变化

建议恢复后先做：

- 文档回扫
- 设计口径再确认

再决定是否按原方案直接续做。

## 6. 当前最重要的提醒

- 不要删除这份暂停备份目录
- 不要只恢复数据库而不恢复代码与文档
- 不要在当前主线需求分支上直接混做企业隔离恢复

## 7. 一句话收口

后续如果需要重启企业隔离计划，最稳的方式就是：**以 `ai-code/.safety_backups/enterprise_isolation_pause_2026-04-29_154001` 为唯一恢复锚点，新开恢复分支，先恢复代码与文档，再恢复数据库和本地验证入口，最后按原专题文档继续推进。**
