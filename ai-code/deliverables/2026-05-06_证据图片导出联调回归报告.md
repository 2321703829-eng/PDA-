# 2026-05-06 证据图片导出联调回归报告

## 1. 回归目标

本轮联调回归重点验证以下内容：

1. 证据图片批量导出任务能否真实生成 ZIP
2. ZIP 内是否包含 `index.csv`
3. `manifest.json` 的 `entry_type` 是否写入真实入口
4. 图片级导出重构后的结果口径是否生效
5. 历史缺图样本是否会正确进入 `partial_failed`

## 2. 联调环境

- 服务器：`192.168.0.17`
- 容器：`odoo-lite-test-v19-web`
- 数据库：`odoo_logistics_webtest_v19`
- 用户上下文：`admin`

## 3. 回归结果一：新上传图片样本导出通过

### 3.1 样本

联调库中选取了 3 条 `image_count > 0` 的证据记录：

- `177`
- `174`
- `172`

### 3.2 真实任务结果

- `task_no = EXT260506-00006`
- `status = success`
- `success_count = 3`
- `fail_count = 0`
- `skipped_count = 0`
- `download_ready = true`
- `download_name = TSL-EXPORT-EVIDENCE-IMAGE-20260506-054213.zip`
- `download_size = 270600`

### 3.3 ZIP 验证

ZIP 实际包含：

- `YD2026042900005/evidence_172/001_bw77mtWFw8aCe2a820634f5228ad02479050c9fc2b06.jpg`
- `YD2026042900006/evidence_174/001_cWpP3jq7BAe0c4d55eed136cb2a0a7708f84a3305fba.jpg`
- `YD2026042900009/evidence_177/001_Ye1NjMIc-xcCc4d55eed136cb2a0a7708f84a3305fba.jpg`
- `index.csv`
- `manifest.json`

### 3.4 manifest 验证

`manifest.json` 已写入真实入口：

- `object_type = evidence_image_bundle`
- `entry_type = from_evidence`

### 3.5 index.csv 验证

`index.csv` 已生成，字段覆盖：

- `batch_no`
- `waybill_no`
- `trace_event_id`
- `trace_event_name`
- `evidence_id`
- `image_id`
- `image_access_key`
- `archive_name`

结论：

- 新上传且结构完整的证据图片导出链路正常
- ZIP / `index.csv` / `manifest.json` 都已真实生成

## 4. 联调库权限问题：已修复

在补第二轮“历史缺图样本”回归时，联调库先暴露出一层额外问题：

- `admin` 用户未命中 `logistics_trace_evidence.group_logistics_trace_manager`
- 导致对部分历史 `logistics.trace.evidence` 记录触发 `AccessError`

真实报错是：

- `Administrator (id=2) 对 logistics.trace.evidence 没有「读取」权限`

处理动作：

- 已将联调库 `admin` 用户补入：
  - `logistics_trace_evidence.group_logistics_trace_manager`

结论：

- 联调库 evidence 读取权限问题已收掉

## 5. 回归结果二：历史缺图混合样本回归通过

### 5.1 样本

本轮使用“1 条正常 + 1 条历史缺图”混合样本：

- 正常样本：`evidence_id = 172`
- 历史缺图样本：`evidence_id = 150`

样本快照：

- `172`
  - `image_count = 1`
  - `image_access_key = img_389c77baf97a90c1a6214c91b85ad26b`
- `150`
  - `image_count = 1`
  - `image_access_key = img_0416c9e2dab09a0b1313d7b2c8bf00dc`
  - 但实际导出时无可读图片内容

### 5.2 真实任务结果

- `task_no = EXT260506-00008`
- `status = partial_failed`
- `success_count = 1`
- `fail_count = 0`
- `skipped_count = 1`
- `download_ready = true`
- `download_name = TSL-EXPORT-EVIDENCE-IMAGE-20260506-055513.zip`

### 5.3 ZIP 验证

ZIP 实际包含：

- `YD2026042900005/evidence_172/001_bw77mtWFw8aCe2a820634f5228ad02479050c9fc2b06.jpg`
- `index.csv`
- `manifest.json`

说明：

- 正常图片仍然被成功打进 ZIP
- 历史缺图记录没有拖垮整个导出包生成

### 5.4 行级结果

行 1：

- `status = success`
- `Evidence - 2026-04-30 08:42:25`
- `Matched Waybill 1 / Exported Evidence 1 / Exported Images 1.`

行 2：

- `status = skipped`
- `证据 - 2026-04-30 07:37:48`
- `Evidence 证据 - 2026-04-30 07:37:48 has no readable evidence images to export.`

### 5.5 错误明细

错误明细已落出：

- `error_code = EXPORT_TARGET_NO_DOWNSTREAM_DATA`
- `error_stage = data_collect`
- `error_message = Evidence 证据 - 2026-04-30 07:37:48 has no readable evidence images to export.`

### 5.6 index.csv 验证

`index.csv` 中只保留了真正写入 ZIP 的图片记录，没有把历史缺图记录伪装成成功图片。

## 6. 当前结论

已经确认没问题的：

1. 证据图片批量导出能真实生成 ZIP
2. ZIP 中确认包含 `index.csv`
3. `manifest.json.entry_type` 已按真实入口写入 `from_evidence`
4. 新上传图片样本导出链通过
5. 联调库 `admin` 的 evidence 读取权限问题已修正
6. 历史缺图混合样本导出时，任务状态会正确收口为 `partial_failed`
7. 历史缺图记录不会阻止 ZIP 生成

## 7. 当前仍建议人工确认的最后一步

虽然接口和任务结果已经通过，但页面级还建议再人工确认一次：

1. 导出结果页是否正确展示：
   - `Partial Failed / 部分失败`
   - `未导出-数据缺失`
2. 结果页图片级摘要是否与任务 `EXT260506-00008` 一致
3. 从证据列表发起后，结果页返回入口是否正确回到证据列表
