# 2026-04-28 企业隔离 `P0` 本地环境首轮执行记录

## 1. 执行目的

本次执行不是正式 `P0` 通过验收，而是先判断：

- 当前本地开发环境是否已经具备 `P0` 正式验证条件
- 还缺哪些环境前置项
- 下一步应该补哪一层

## 2. 本次执行环境

| 项目 | 结果 |
| --- | --- |
| 环境名称 | `local_odoo_logistics_dev` |
| Odoo 地址 | `http://127.0.0.1:8069` |
| 配置文件 | `D:\Desktop\Odoo\odoo_local.conf` |
| 数据目录 | `D:\Desktop\Odoo\.odoo_data` |

## 3. 本次已核对事实

### 3.1 Odoo 配置现状

从 `odoo_local.conf` 实际确认：

- `http_interface = 127.0.0.1`
- 未配置 `dbfilter`
- 未配置 `list_db`
- 未配置 `proxy_mode`

补充说明：

- 已新增 `kebi` 专用验证配置 [odoo_p0_kebi.conf](</d:/Desktop/Odoo/ai-code/.runtime/odoo_p0_kebi.conf>)
- 该配置运行在 `127.0.0.1:8070`
- 其中已启用：
  - `dbfilter = ^tenant_%d$`
  - `list_db = False`

### 3.2 数据库现状

从 PostgreSQL 实际确认当前存在：

- `odoo_logistics_dev`
- `odoo_logistics_phase4_v1`
- `postgres`
- `tenant_kebi`
- `tenant_kebi_restore_check`

### 3.3 文件存储现状

从 `data_dir` 实际确认：

- `filestore/odoo_logistics_dev` 存在
- `filestore/odoo_logistics_phase4_v1` 存在
- `filestore/tenant_kebi` 存在
- `filestore/tenant_kebi_restore_check` 存在

## 4. 对照 `P0` 清单后的判断

### 4.1 已具备的条件

- [x] 已有可用 Odoo 环境
- [x] 已有多个业务数据库
- [x] 已有可用文件存储目录
- [x] 已可继续承担模块开发与联调

### 4.2 当前缺失的关键条件

- [x] 已确定正式 `tenant_code = kebi`
- [x] 已确定目标数据库命名 `tenant_kebi`
- [x] `tenant_kebi` 数据库已创建
- [x] 已建立本地测试域名 `kebi.lvh.me:8070`
- [x] 已配置 `dbfilter`
- [x] 已形成单库备份恢复验收记录
- [ ] 尚未形成独立反向代理部署版入口
- [ ] 未确认数据库列表入口是否已完全收口

## 5. 当前结论

### 5.1 是否通过 `P0`

- 结论：**本地最小验证通过，但正式 `P0` 仍未完全通过**

当前已经完成：

- `tenant_kebi` 生库
- 独立 filestore
- 本地子域名命中验证
- `dbfilter` 验证
- 单库备份恢复验证

当前仍未完全满足正式 `P0`，主要是因为：

- 尚未形成独立反向代理部署版入口
- 非命中租户仍会落到 selector 页，数据库入口未完全收口

### 5.2 是否还能继续推进

- 可继续推进 `P1` 开发与联调：**可以**
- 可直接宣告企业隔离 `P0` 已成立：**不可以**

## 6. 下一步最小动作

如果现在要真正进入 `P0` 执行，建议按最小闭环走：

1. 以 `kebi.lvh.me:8070` 作为当前本地验证入口继续做人工访问回归
2. 决定是否需要补独立反向代理部署入口
3. 继续验证数据库选择页的完全收口方式
4. 将当前结果回填到正式验收记录和交付材料

## 7. 一句话收口

当前本地环境已经把 `kebi / tenant_kebi` 这套 `tenant_code + db + domain + dbfilter` 的最小闭环补出来了，但还没有到“正式代理部署版 `P0` 完全通过”的程度；接下来要补的是入口收口和正式部署形态，而不是重新搭建主链。
