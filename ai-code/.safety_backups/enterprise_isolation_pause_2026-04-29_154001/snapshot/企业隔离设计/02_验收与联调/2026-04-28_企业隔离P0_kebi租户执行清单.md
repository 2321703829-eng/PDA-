# 2026-04-28 企业隔离 `P0` `kebi` 租户执行清单

## 1. 文档定位

- 本文用于把首个正式 `P0` 测试租户从“目标参数”推进到“真实可访问租户”。
- 本文只面向当前先采用的方案 A：`子域名 -> dbfilter -> 数据库`。
- 当前固定测试租户信息：
  - `tenant_code = kebi`
  - `database = tenant_kebi`

## 2. 当前状态

### 2.1 已确定项

- [x] 已确定测试租户编码 `kebi`
- [x] 已确定目标数据库名 `tenant_kebi`
- [x] 已完成本地开发环境首轮摸底
- [x] 已确认当前 `odoo_local.conf` 未配置 `dbfilter`
- [x] 已确认当前环境仍是 `127.0.0.1:8069` 本地开发访问

### 2.2 当前未完成项

- [x] `tenant_kebi` 数据库已创建
- [x] `filestore/tenant_kebi` 已生成
- [x] 已确定本地测试访问域名 `kebi.lvh.me:8070`
- [ ] 独立反向代理与 `Host` 透传尚未单独部署
- [x] `dbfilter` 已配置
- [x] 有效租户入口上的数据库列表入口已收口
- [ ] 正式代理白名单部署版入口尚待验证
- [x] 单库备份恢复已形成正式记录

## 3. 执行步骤

### 步骤 1：准备目标数据库

- [x] 创建或复制目标数据库 `tenant_kebi`
- [x] 确认数据库可被 Odoo 识别
- [ ] 记录数据库来源：
  - 全新安装
  - 基于现有开发库复制
  - 其他方式

本步骤完成标志：

- PostgreSQL 中存在 `tenant_kebi`
- Odoo 可对 `tenant_kebi` 执行初始化或模块升级

### 步骤 2：准备文件存储

- [x] 确认 `filestore/tenant_kebi` 已生成或已初始化
- [x] 确认数据库与 filestore 对应关系清晰

本步骤完成标志：

- `tenant_kebi` 有独立文件存储目录

### 步骤 3：准备访问域名

- [x] 确定 `base_domain`
- [x] 确定 `kebi` 的正式测试访问域名
- [x] 在登记单中回填真实域名

建议记录格式：

- `tenant_code = kebi`
- `database = tenant_kebi`
- `domain = kebi.lvh.me:8070`

### 步骤 4：配置反向代理与 Host 透传

- [x] 已确认 `Host = kebi.lvh.me` 时可命中 `tenant_kebi`
- [ ] 如需独立反向代理部署，仍需补正式入口层
- [x] 当前本地验证不依赖 `proxy_mode`

本步骤完成标志：

- 访问 `kebi.lvh.me:8070` 能稳定到达目标 Odoo 服务

### 步骤 5：配置 dbfilter

- [x] 按当前方案 A 配置 `dbfilter`
- [x] 确认 `kebi.lvh.me:8070` 只命中 `tenant_kebi`
- [x] 确认不会误直接命中其他业务库；本地泛测试子域名场景不直接等同正式代理白名单部署

本步骤完成标志：

- `kebi` 域名访问只进入 `tenant_kebi`

### 步骤 6：验证数据库入口收口

- [x] 验证有效租户入口上的数据库选择页已重定向回登录页
- [x] 验证 `/web/database/manager` 已关闭
- [x] 验证 `/web/database/list` 已关闭
- [x] 已验证非命中租户不会直接进入其他业务库

本步骤完成标志：

- 非预期数据库入口已被有效收口，且数据库列表不暴露

### 步骤 7：验证备份恢复

- [x] 对 `tenant_kebi` 做一次单库备份
- [x] 做一次恢复验证
- [x] 恢复后验证核心业务表已可读

本步骤完成标志：

- 已形成一轮正式备份恢复记录

## 4. 验收口径

`kebi` 租户进入 `P0` 通过态，至少要求以下全部成立：

- [x] 有真实数据库 `tenant_kebi`
- [x] 有独立 filestore
- [x] 有真实访问域名
- [ ] 有独立反向代理与 `Host` 透传
- [x] 有正式 `dbfilter`
- [x] 域名只能命中 `tenant_kebi`
- [x] 有效租户入口上的数据库列表入口已收口
- [ ] 正式代理白名单部署版入口已验证
- [x] 已形成单库备份恢复记录

## 5. 当前一句话结论

`kebi / tenant_kebi` 已经从“概念示例”升级为当前 `P0` 首个本地可访问、可备份恢复验证的测试租户；当前剩余的关键差距不在业务主链，而在独立入口层和数据库列表入口的完全收口。

## 6. Visible Marker

- The tenant now includes a direct visual marker for manual verification.
- Default company name:
  - before: `My Company`
  - now: `KEBI test tenant`
- Purpose:
  - help operators confirm they are inside `tenant_kebi`
  - reduce confusion between local dev databases during `P0` validation

## 7. Reverse Proxy Validation Result

The local reverse-proxy-style validation has now been completed:

- proxy listen:
  - `127.0.0.1:8090`
- tenant domain:
  - `kebi.tianshu.test`
- dev domain:
  - `dev.tianshu.test`

Validated access:

- `http://kebi.tianshu.test:8090/web/login`
- `http://dev.tianshu.test:8090/web/login`

Validated closure behavior:

- `http://kebi.tianshu.test:8090/web/database/selector` returns to `/web/login`

Current conclusion:

- `kebi / tenant_kebi` has completed not only the direct local port validation, but also a host-based reverse-proxy route validation
- this can be treated as the current local `P0` route verification completion record
