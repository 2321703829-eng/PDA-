# 2026-04-29 `P0` 正式部署与正式验收剩余工作清单

## 1. 文档定位

- 本文用于收口当前企业隔离专题在 `P0` 阶段剩余的正式环境工作。
- 当前默认继续采用 **方案 A：子域名 -> dbfilter -> 数据库**。
- 本文只讨论“正式部署”和“正式验收”剩余事项，不再重复已经完成的本地验证项。

## 2. 当前判断

当前 `P0` 的本地验证版已经完成：

- `tenant_kebi` 独立数据库
- 独立 `filestore`
- `dbfilter` 命中验证
- 单库备份恢复验证
- 有效租户入口下的数据库入口收口
- 本地测试域名 + 本地反向代理验证

因此当前 `P0` 剩余工作，已经从“本地可行性验证”转向“正式环境部署与正式验收”。

## 3. 正式部署剩余工作

### 3.1 正式域名准备

- [x] 确定 `kebi` 的正式测试子域名：`kebi.tianshu.cn`
- [ ] 完成 DNS 解析
- [ ] 记录域名与租户映射关系

建议至少登记：

- `tenant_code`
- 数据库名
- 子域名
- 生效环境
- 负责人

### 3.2 正式反向代理部署

- [x] 已形成正式反向代理配置说明稿
- [x] 已形成发给运维的正式部署执行指令稿
- [ ] 在正式测试环境或准生产环境部署反向代理
- [ ] 确保子域名请求能转发到目标 Odoo 服务
- [ ] 确保 `Host` 头透传
- [ ] 确保上传、超时、长连接等代理参数满足 Odoo 使用要求

最低要求：

- `kebi.xxx` 访问时只能进入 `tenant_kebi`
- 不应回落到其他数据库

### 3.3 正式 `dbfilter` 配置

- [x] 已准备正式 `dbfilter` 建议值：`^tenant_%d$`
- [ ] 在正式运行配置中写入 `dbfilter`
- [ ] 确认正式配置文件已生效
- [ ] 确认非目标域名不会误命中业务库

最低要求：

- `kebi` 子域名只能命中 `tenant_kebi`

### 3.4 HTTPS 与证书

- [ ] 如果正式环境要求 HTTPS，则补齐证书申请与部署
- [ ] 验证 HTTPS 访问可用
- [ ] 验证 HTTPS 下登录、跳转、下载与图片访问无异常

## 4. 正式验收剩余工作

### 4.1 正式入口验收

- [ ] 用正式测试域名直接访问登录页
- [ ] 验证登录页能正常打开
- [ ] 验证不会暴露数据库选择页
- [ ] 验证 `/web/database/manager` 不可用
- [ ] 验证 `/web/database/list` 不可用

建议记录：

- 访问地址
- 实际落库
- 页面现象
- 是否通过

### 4.2 正式业务最小链路验收

- [ ] 登录 `tenant_kebi`
- [ ] 打开运单页
- [ ] 验证留痕主链可用
- [ ] 验证证据主链可用
- [ ] 验证异常主链可用
- [ ] 验证导入中心入口可用

目标不是做全量业务回归，而是证明：

- 企业隔离成立后，当前 `P1` 主业务链在正式入口上仍然成立

### 4.3 正式权限验收

- [ ] 只读用户验收
- [ ] 图片导入 / 现场人员验收
- [ ] 调度 / 管理验收

建议最低验收点：

- 只读账号能看不能写
- 图片导入 / 现场人员能导图、提留痕，但不能关异常
- 调度 / 管理能完成异常处理闭环

### 4.4 正式备份恢复验收

- [ ] 确认正式环境备份机制
- [ ] 确认数据库与 `filestore` 对应关系
- [ ] 如条件允许，做一轮正式环境备份恢复演练

最低要求：

- 能说明“怎么备份”
- 能说明“怎么恢复”
- 能证明“恢复后租户链路仍可用”

## 5. 正式验收材料剩余工作

- [x] 已准备正式验收表头与回填模板
- [ ] 回填正式环境参数
- [ ] 回填正式域名
- [ ] 回填正式代理方式
- [ ] 回填正式入口验证结果
- [ ] 回填正式权限验收结果
- [ ] 回填正式备份恢复验收结果
- [ ] 给出最终结论：是否允许认定 `P0` 正式通过

建议最终材料至少包含：

- 环境名称
- 域名
- 数据库
- 代理方式
- 通过项
- 未通过项
- 风险项
- 结论

## 6. 当前不再列为 `P0` 主缺口的内容

以下内容当前不再作为 `P0` 主缺口：

- 本地 `kebi / tenant_kebi` 数据库准备
- 本地 `filestore` 准备
- 本地 `dbfilter` 验证
- 本地备份恢复验证
- 本地测试域名验证
- 本地反向代理验证

这些内容已经可以视为当前 `P0` 本地验证基线的一部分。

## 7. 一句话收口

当前 `P0` 剩余工作已经非常明确：**继续采用方案 A，补齐正式子域名、正式反向代理、正式 `dbfilter`、正式入口验收和正式验收记录**；主线不再是“能不能隔离”，而是“怎么在正式环境把当前这套隔离链交付落地”。
