# 2026-04-28 企业隔离 `P0` 真实环境参数登记单

## 1. 文档定位

- 本文用于登记当前真实测试环境中的企业隔离 `P0` 路由与数据库参数实值。
- 本文记录的是当前已经实际观察到的环境状态，不等于最终目标态。
- 当前这份登记单对应的是本地开发环境首轮摸底结果，并已纳入首个正式测试租户目标信息。

## 2. 当前环境基础信息

| 项目 | 当前值 |
| --- | --- |
| 环境名称 | `local_odoo_logistics_dev` |
| 环境类型 | 开发 |
| 填写日期 | `2026-04-28` |
| 填写人 | AI + 人工复核协同 |
| 基础域名 `base_domain` | 本地验证：`lvh.me`；正式测试口径：`tianshu.cn` |
| Odoo 服务地址 | `http://127.0.0.1:8070` |
| 反向代理地址 | 本地验证已存在 `127.0.0.1:8090`；正式测试口径待按 `kebi.tianshu.cn` 落地 |
| 当前 `dbfilter` | 本地验证：`^(tenant_kebi|tenant_%d)$`；正式测试建议：`^tenant_%d$` |
| 是否关闭数据库选择页 | 有效租户入口已收口；本地泛测试子域名场景仍不等同正式代理白名单部署 |
| `proxy_mode` | 未配置 |
| `http_interface` | `127.0.0.1` |
| 备注 | 当前为单机本地开发环境，主要用于模块开发与联调；已确定首个 `P0` 正式测试租户编码为 `kebi` |

## 3. 当前数据库现状

当前 PostgreSQL 中已确认存在以下非模板数据库：

| 数据库名 | 备注 |
| --- | --- |
| `odoo_logistics_dev` | 当前主开发库 |
| `odoo_logistics_phase4_v1` | 现有历史业务开发库 |
| `postgres` | PostgreSQL 系统库 |
| `tenant_kebi` | 已创建，来源为 `odoo_logistics_dev` 复制 |

## 4. 当前文件存储现状

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `filestore/odoo_logistics_dev` | 存在 | 当前主开发库文件存储已存在 |
| `filestore/odoo_logistics_phase4_v1` | 存在 | 历史业务开发库文件存储已存在 |
| `filestore/tenant_kebi` | 存在 | 已基于 `odoo_logistics_dev` 对应 filestore 复制生成 |
| `filestore/tenant_kebi_restore_check` | 存在 | 备份恢复验证生成的临时校验 filestore |

## 5. 当前租户登记表

> 当前环境尚未按 `tenant_code -> 子域名 -> 数据库` 的正式 `P0` 口径建立真实租户映射，本表先同时登记现有本地业务库与已确定的目标测试租户。

| 租户显示名 | `tenant_code` | 数据库名 | 访问域名 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 当前主开发库 | 未设 | `odoo_logistics_dev` | `127.0.0.1:8069` | active | 现阶段通过本地地址直接访问 |
| 历史业务开发库 | 未设 | `odoo_logistics_phase4_v1` | 未单独配置 | active | 当前未纳入正式企业隔离映射 |
| `kebi` 测试租户 | `kebi` | `tenant_kebi` | 本地：`kebi.lvh.me:8070`；正式测试：`kebi.tianshu.cn` | active | 已完成本地最小验证；已定正式测试子域名口径 |

## 6. 配置项核对

### 6.1 已确认项

- [x] Odoo 监听地址为 `127.0.0.1`
- [x] 当前存在多个业务数据库
- [x] 当前主开发库已有对应 filestore
- [x] 已为 `kebi` 专用验证实例配置 `dbfilter = ^tenant_%d$`
- [x] 当前没有配置 `proxy_mode`
- [x] 已确定正式 `tenant_code = kebi`
- [x] 已确定目标数据库命名 `tenant_kebi`
- [x] 已创建 `tenant_kebi` 数据库
- [x] 已生成 `tenant_kebi` 独立 filestore

### 6.2 未满足 `P0` 正式要求的项

- [x] 已建立本地验证域名 `kebi.lvh.me:8070`
- [x] 已形成域名到数据库一对一命中验证
- [x] 已形成单库备份恢复记录
- [ ] 尚未形成独立反向代理部署版入口
- [x] 有效租户入口上的 selector / manager / list 已收口
- [ ] 正式代理白名单部署版入口尚未形成
- [x] 已定正式测试子域名 `kebi.tianshu.cn`
- [x] 已准备正式反向代理配置说明稿
- [x] 已准备正式 `dbfilter` 建议值与正式验收表头

## 7. 当前访问与验证记录

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 当前主库可正常访问 | 通过 | `http://127.0.0.1:8069` 可用于开发联调 |
| 多库现状存在 | 通过 | 本地库中已存在两个业务数据库 |
| `tenant_kebi` 已存在 | 通过 | 当前 PostgreSQL 中已存在 `tenant_kebi` |
| 子域名命中数据库 | 通过 | `Host = kebi.lvh.me` 时可稳定进入 `tenant_kebi` 登录页 |
| `dbfilter` 路由验证 | 通过 | `dbfilter = ^tenant_%d$`，`kebi` 命中、`foo` 不命中 |
| 正式 `P0` 租户映射验证 | 部分通过 | 本地最小命中验证已通过，且有效租户入口上的 selector / manager / list 已收口，但尚未形成独立反向代理部署版入口 |
| 单库备份恢复验证 | 通过 | 已生成 dump，并恢复到 `tenant_kebi_restore_check` 完成校验 |

## 8. 当前环境结论

- 当前环境是否可用于代码开发与联调：
  - `是`
- 当前环境是否已满足 `P0` 正式验收：
  - `部分满足`
- 当前环境是否可直接作为企业隔离正式底盘样板：
  - `否`

当前还缺：

- 独立反向代理部署版入口
- 正式代理白名单部署版入口的完整收口记录
- 正式 DNS 与正式代理发布记录

## 9. 当前建议

当前环境可以继续承担：

- 模块编码
- 页面联调
- 权限回归

但如果要真正推进 `P0`，下一步必须切到：

1. 用已定的正式测试子域名 `kebi.tianshu.cn` 准备正式 DNS 与正式代理发布
2. 将正式反向代理配置说明稿交给运维落地
3. 将正式 `dbfilter` 配置项写入正式运行配置并验证
4. 按正式验收表头回填入口、权限与备份恢复结果

## 10. 一句话收口

当前这套本地环境已经把 `kebi / tenant_kebi` 从“目标参数”推进成了“本地可访问租户”，并已跑通域名命中、dbfilter、单库备份恢复，且在有效租户入口上收住了 selector / manager / list；当前已经进一步定下正式测试子域名 `kebi.tianshu.cn`，并补齐正式反向代理配置说明与正式验收表头，但若要把它视为正式 `P0` 通过态，还需要继续补正式 DNS、正式代理发布和正式收口记录。

## 11. Visible Marker

- `tenant_kebi` now carries an obvious visual marker for human verification.
- The default company name in this tenant has been changed from `My Company` to `KEBI test tenant`.
- Practical meaning:
  - when users log in to the `tenant_kebi` instance, they can identify the tenant by visible company naming rather than relying only on URL and memory.
  - this marker is an environment identification aid for local `P0` validation and does not change the current `P0/P1` business design boundary.

## 12. Local Entry Split

Current local entry split is now:

- `http://127.0.0.1:8069/web/login`
  - original multi-db local development gateway
- `http://127.0.0.1:8070/web/login`
  - fixed entry for `tenant_kebi`
- `http://127.0.0.1:8071/web/login`
  - fixed entry for `odoo_logistics_dev`

Practical meaning:

- `8070` is used when validating enterprise isolation around the dedicated `kebi` tenant.
- `8071` is used when returning to the original main development database without relying on previous browser session memory.

## 13. Local Reverse Proxy Validation

The local environment now also includes a dedicated reverse-proxy-style validation layer:

- reverse proxy listen: `127.0.0.1:8090`
- tenant host:
  - `kebi.tianshu.test -> tenant_kebi`
  - `dev.tianshu.test -> odoo_logistics_dev`

Validated results:

- `http://kebi.tianshu.test:8090/web/login` -> login page available
- `http://dev.tianshu.test:8090/web/login` -> login page available
- `http://kebi.tianshu.test:8090/web/database/selector` -> redirected back to `/web/login`

Current judgment:

- the local environment has now completed one round of host-based reverse-proxy route verification
- this is sufficient as a local `P0` validation record
- it still does not equal a formal production proxy deployment record
