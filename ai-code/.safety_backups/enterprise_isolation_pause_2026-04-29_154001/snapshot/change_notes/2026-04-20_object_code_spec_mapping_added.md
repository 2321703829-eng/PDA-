# 2026-04-20 对象与编码统一规范补充交叉映射与落地清单

## 本次变更

- 更新 `仓管模块设计/02_跨模块规范/01_对象与编码/00_对象与编码统一规范草稿.md`
- 新增三章：
  - `16. OBJ-* 到数据库字段编号的逐对象回填表`
  - `17. OBJ-* 到状态稿 ST-* / EST-* 的逐对象编号映射`
  - `18. OBJ-* 到真实模块文件树 / 方法 / 页面对象的逐项落地清单`
- 同步顺延：
  - `当前建议联读专题`
  - `v0.1 收口与后续补充方向`

## 当前收口

这轮已经把对象与编码稿从“原则层”推进到了：

- 对象编号到数据库字段的回填层
- 对象编号到状态编号的映射层
- 对象编号到真实模块文件 / 方法 / 页面对象的落地层

## 下一步建议

- `OBJ-*` 到数据库字段编号的逐字段编号映射
- `OBJ-*` 到权限矩阵、执行页按钮矩阵的逐对象交叉映射
- `OBJ-*` 到真实开发任务编号 / 文件路径 / 方法名 / 页面对象的逐项回填状态表
