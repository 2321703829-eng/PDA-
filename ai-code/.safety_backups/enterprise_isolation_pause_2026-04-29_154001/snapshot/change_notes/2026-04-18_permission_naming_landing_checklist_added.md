# 2026-04-18 权限与角色规范补充落地命名清单

## 本次变更

补充 `仓管模块设计/01_模块设计/08_设置与规则/00_权限与角色规范草稿.md`：

- 新增 `20. 角色、状态、按钮到具体文件 / 方法 / 组件的落地命名清单`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `21`
- 同步更新目录索引、章节总览与阅读主线说明

## 本次补充的重点

### 1. 把权限专题继续推进到文件 / 方法 / 组件命名层

本次不再只讨论：

- XML / Python / JS 三层职责

而是继续明确：

- 角色更适合落到哪些安全文件和 group XML ID
- 状态更适合落到哪些模型字段和模型方法
- 详情页按钮更适合落到哪些 XML / Python 对象
- 执行页按钮更适合落到哪些 JS / OWL / Python 对象

### 2. 固定了一批当前推荐的命名方向

本次补进了一组更接近实现的命名方向，例如：

- `logistics_wms.group_wms_operator`
- `action_start_receipt`
- `action_open_pick_page`
- `confirmCurrent / confirm_current`
- `finishRecord / finish_record`

这样后面进入开发阶段时，命名不会再从零开始发散。

### 3. 补了一张文件级落位清单

本次还把权限相关对象大致映射到了：

- `views/*.xml`
- `security/*.xml`
- `models/*.py`
- `security/ir.model.access.csv`
- `static/src/js/**`
- `static/src/xml/**`

让这份权限专题从“规则与实现层”继续推进到了“对象与文件层”。

## 当前意义

这次补充后，这份权限专题已经不只是在规定“权限该怎么理解”，而是开始说明“这些权限大致应写到哪里、怎么命名”。  
后续如果继续补“权限规则到具体模块文件树的总索引”，这份专题就可以进一步成为真正的开发落地清单。
