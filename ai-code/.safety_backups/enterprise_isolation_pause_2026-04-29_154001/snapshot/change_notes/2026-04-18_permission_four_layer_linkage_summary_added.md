# 2026-04-18 权限与角色规范补充四层联动总表

## 本次变更

补充 `仓管模块设计/01_模块设计/08_设置与规则/00_权限与角色规范草稿.md`：

- 新增 `17. 角色、状态、按钮、记录规则四层联动总表`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `18`
- 同步更新目录索引、章节总览与阅读主线说明

## 本次补充的重点

### 1. 把权限专题前面已经拆开的几层真正压成一页总览

本次把前面已经陆续补过的几层统一收成了一页总表：

- 角色层
- 状态层
- 按钮层
- 记录规则层

这样后面再看按钮显隐或按钮可用性时，不需要在多份章节之间来回跳。

### 2. 明确了四层之间的职责分工

本次明确：

- 角色层决定“谁默认有资格”
- 状态层决定“当前时机下能不能做”
- 按钮层决定“在哪里做”
- 记录规则层决定“能不能对这条数据做”

这让权限专题、状态机专题、页面结构专题之间的边界更清楚了。

### 3. 补了一条可直接复用的判断链示例

本次用“开始上架”举了一个四层联动的典型判断链，后面做：

- 前端按钮显隐
- 后端按钮校验
- 评审时的权限核对

都可以直接沿这条链来对。

## 当前意义

这次补充后，这份权限专题已经不只是在列：

- 角色
- 菜单
- 页面
- 动作

而是开始提供一种真正可落到实现的统一校对方式。  
后续如果继续补“与前端显隐实现、后端校验实现之间的落地对照表”，这份权限稿就可以直接成为权限落地评审时的一页总底稿。
