# 2026-04-18 权限与角色规范补充执行页组件专题映射校对表

## 本次变更

补充 `仓管模块设计/01_模块设计/08_设置与规则/00_权限与角色规范草稿.md`：

- 新增 `24. 与执行页组件命名专题之间的一对一映射校对表`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `25`
- 同步更新结尾收口中的已固定重点与后续方向

## 本次补充的重点

### 1. 把权限专题和组件命名专题正式拉通

本次不再继续新增一层全新的规则，而是开始做专题之间的校对：

- 权限专题负责定义边界
- 执行页组件命名专题负责定义载体

通过这次新增的一对一映射表，开始把两份文档真正拉到一起。

### 2. 明确了几组最关键的对应关系

本次重点对齐了：

- 入口按钮 <-> `client action`
- 过程按钮 <-> 右侧执行组件
- 权限辅助判断 <-> shared `service / hook`
- 提示与空状态 <-> 反馈层组件

这样后面实现执行页时，就不会再出现“权限边界定在这里，但组件层承载在另一层”的错位。

### 3. 固定了两份专题的职责边界

本次进一步明确：

- 权限专题定边界
- 组件专题定载体
- 一对一映射表定两者之间的对齐关系

## 当前意义

这次补充后，这份权限专题已经不只是单独成文，而是开始和“执行页组件命名与文件映射草稿”形成交叉校对关系。  
后续如果继续整理总规范层的 `v0.1` 基线清单，这两份专题就可以作为一组前端交互与权限实现的配套基线一起引用。
