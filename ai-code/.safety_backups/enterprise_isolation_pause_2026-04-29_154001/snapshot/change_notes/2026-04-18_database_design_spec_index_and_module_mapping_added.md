# 2026-04-18 数据库设计规范稿补更细的索引清单与模块映射

## 本次变更

- 更新 `仓管模块设计/02_跨模块规范/04_接口与数据/01_数据库设计规范草稿.md`
- 在数据库专题中新增：
  - `更细的索引 / 唯一约束清单`
  - `模型到具体模块 / 文件树的映射清单`
- 同步调整：
  - `目录索引`
  - `章节总览`
  - 阅读主线
  - 后半段章节编号
  - 小节编号

## 本次新增内容

### 1. 更细的索引 / 唯一约束清单

- 将一期更值得优先关注的索引对象细分为：
  - 主数据对象
  - 主单对象
  - 任务对象
- 将当前最值得优先写死的唯一约束方向收成独立清单

### 2. 模型到具体模块 / 文件树的映射清单

- 将主数据、主单、执行记录、任务、证据元数据分别挂到建议模块
- 给出当前建议的文件方向
- 同步补出原生扩展对象的推荐文件方向

## 当前结果

- 数据库专题已从“原则 + 模型总表 + 字段总表”继续推进到“索引 / 唯一约束 + 模块 / 文件树映射”层
- 当前更适合作为数据库评审、模型实现和模块落位讨论时的统一入口
