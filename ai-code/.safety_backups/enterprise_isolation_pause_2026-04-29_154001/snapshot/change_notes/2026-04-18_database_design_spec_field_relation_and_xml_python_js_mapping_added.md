# 2026-04-18 数据库设计规范补字段关系总表与三层实现映射

## 本次变更

- 继续深化 `仓管模块设计/02_跨模块规范/04_接口与数据/01_数据库设计规范草稿.md`
- 新增 `正式 ER 图前的字段关系总表`
- 新增 `到 XML / Python / JS 三层实现对象的映射草稿`
- 同步调整目录索引、章节总览、阅读主线和 `v0.1` 收口说明

## 目的

- 在正式绘制 ER 图前，先把一期核心对象之间最关键的关系字段稳定下来
- 把数据库对象和工程实现层之间的对应关系提前拉通，避免后续模型已定但 XML / Python / JS 落点分散

## 当前结果

- 数据库专题现已补齐：
  - 主数据、主单、执行记录、任务与证据对象的字段关系总表
  - 数据库对象到 XML / Python / JS 三层实现对象的映射方向
- `v0.1` 基线范围同步扩大到：
  - 字段关系总表
  - 三层实现映射草稿

## 后续建议

- 下一步可继续补：
  - 更细的 XML / Python / JS 三层实现对象落地清单
  - 正式 ER 图图形版或关系表版
