# 2026-04-17 仓管关联基数与字段类型结论补入讨论稿

## 本次变更

更新文档：

- `仓管模块设计/00_导航与总纲/仓管模块设计启动讨论稿.md`

补充了本轮已确认的内容：

- 仓管与 `stock` 关联基数的总体设计原则
- 哪些关系必须从一开始就按一对多设计
- 哪些关系适合按单一主关联加可扩展反查处理
- 哪些关系以 `Many2one` 为主
- 各主要关系推荐的 `Many2one / One2many` 字段落法
- 当前明确不建议优先使用 `Many2many` 的核心链路

## 变更目的

把仓管业务模型与 `stock` 执行对象之间的关系基数和字段类型沉淀为启动阶段文档，为下一步继续收敛 stock 扩展边界提供稳定基线。

## 当前说明

本次更新仍属于讨论稿阶段，后续仍可继续细化具体模型名、字段命名和执行记录对象是否精简。
