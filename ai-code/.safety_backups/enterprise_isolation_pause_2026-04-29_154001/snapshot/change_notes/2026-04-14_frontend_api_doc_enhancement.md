# 2026-04-14 前端接口总文档增强版补充

## 本次变更

继续增强以下文档：

- [前端接口设计总文档.md](/d:/Desktop/Odoo/ai-code/前端相关设计/02_跨模块规范/01_接口与数据/前端接口设计总文档.md)

## 本次补充内容

在原有“接口用途 + 查询方式 + 字段含义”的基础上，继续补充了：

1. 每个核心接口的请求参数表
   - 类型
   - 是否必填
   - 是否可空
   - 字段含义

2. 每个核心接口的请求示例

3. 每个核心接口的响应示例

4. 常用枚举值定义
   - `current_status`
   - `evidence_status`
   - `exception_status`
   - `severity_level`
   - `risk_level`
   - `trace_type`

## 当前结果

这份文档现在已经更接近可以直接发给后端对接使用的版本，具备：

- 接口名称可读
- 查询方式明确
- 字段语义清楚
- 示例完整
- 枚举口径可对齐

## 后续建议

如果还要继续往下走，下一步最值得补的是：

- 字段级 DTO 结构图
- controller / RPC 路由路径示例
- 典型错误码与错误响应示例
