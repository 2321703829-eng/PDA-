# 2026-04-18 权限与角色规范补充 XML / Python / JS 映射草稿

## 本次变更

补充 `仓管模块设计/01_模块设计/08_设置与规则/00_权限与角色规范草稿.md`：

- 新增 `19. 权限矩阵到 XML / Python / JS 三层实现对象的映射草稿`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `20`
- 同步更新目录索引、章节总览与阅读主线说明

## 本次补充的重点

### 1. 把权限专题继续推进到工程对象层

本次不再只讨论：

- 角色矩阵
- 状态机联动
- 页面按钮边界
- 前后端分工

而是进一步明确：

- 什么优先落 XML
- 什么必须落 Python
- 什么更适合落 JS / OWL

这样后面开始真正写菜单、按钮、client action、模型校验时，就不需要再临时判断职责归属。

### 2. 固定了三层对象的当前推荐分工

本次明确：

- XML 收入口
- JS 收交互
- Python 守规则

这让权限专题、页面结构专题、执行页组件专题之间的连接更自然了。

### 3. 补了一张一页映射总表

本次把：

- 菜单可见性
- 列表 / 详情入口
- 详情页按钮
- 执行页按钮
- 状态合法性
- 记录规则范围
- 只读角色页面态

分别映射到了 XML / Python / JS 三层对象上。

## 当前意义

这次补充后，这份权限专题已经从“规则基线”继续推进到了“实现对象映射基线”。  
后续如果继续补“角色、状态、按钮到具体文件 / 方法 / 组件的落地命名清单”，这份权限稿就可以继续往真正的开发落地清单推进。
