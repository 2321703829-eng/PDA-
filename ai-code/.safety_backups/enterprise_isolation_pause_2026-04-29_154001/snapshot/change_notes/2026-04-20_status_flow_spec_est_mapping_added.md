# 2026-04-20 状态与流程统一规范补充 EST 三层映射

## 本次变更

- 在 `仓管模块设计/02_跨模块规范/02_状态与流程/00_状态与流程统一规范草稿.md` 中新增：
  - `25. EST-* 到数据库字段映射`
  - `26. EST-* 到权限矩阵映射`
  - `27. EST-* 到执行页提示与按钮显隐映射`
- 同步更新了该文档的：
  - `目录索引`
  - `章节总览`
  - `16. v0.1 收口与后续补充方向`

## 当前结果

- 当前状态总规范已经把异常补充状态编号 `EST-*` 继续落到了三层：
  - 数据库字段层
  - 权限矩阵层
  - 执行页交互层

## 备注

- 当前统一口径仍然是：
  - `EST-*` 负责跨专题交叉引用编号
  - `exception_state` 负责数据库实际字段值
- 后续如继续深化，建议优先补：
  - `EST-*` 到日志 / 证据 / 任务状态的映射
  - `EST-*` 到详情页异常区块与执行页反馈层的逐组件映射
