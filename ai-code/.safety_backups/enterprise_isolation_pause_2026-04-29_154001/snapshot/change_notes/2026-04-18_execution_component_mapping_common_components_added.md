# 2026-04-18 执行页组件命名与文件映射专题补充公共组件拆分清单

## 本次变更

补充 `仓管模块设计/01_模块设计/02_执行页组件命名与文件映射草稿.md`：

- 新增 `13. 更细的公共组件拆分清单`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `14`
- 同步更新目录索引、章节总览与阅读主线说明

## 本次补充的重点

### 1. 把公共组件拆成五组

当前固定为：

- 页面壳层
- 左侧待处理区
- 右侧执行区
- 底部信息层
- 反馈层

### 2. 给出了一批可直接沿用的公共组件方向

例如：

- `ExecutionPageLayout`
- `ExecutionTopContextBar`
- `ExecutionPendingTable`
- `ExecutionActionPanel`
- `ExecutionBottomTabs`
- `ExecutionHintBar`
- `ExecutionConfirmDialog`
- `ExecutionUnsavedGuard`

### 3. 固定了公共层与动作页的边界

本次强调：

- 公共层先抽稳定结构层
- 不过早抽过深业务语义层
- 动作差异通过配置、插槽、子组件注入
- 不为四类执行页重复复制几乎相同的容器和反馈逻辑

## 当前意义

这次补充后，这份专题稿已经从：

- 命名规范
- 文件映射
- asset 组织
- 联调校验

继续推进到了：

- 公共组件分层
- 公共壳层与动作差异层边界

后续如果真的开始在 `logistics_wms_web` 中拆执行页前端结构，这一版已经可以直接作为组件分层的基线参考。
