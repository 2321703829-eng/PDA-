# 2026-04-27 first project baseline tag note draft added

## What Changed

Added the first draft of the project baseline tag note:

- added `docs/versioning/baseline_2026-04-27_v0.1.0_tianshu_tag_note.md`

Updated the tag governance doc so the current first baseline naming decision is explicit:

- updated `docs/versioning/version_tag_governance.md`

## Why

The team has now decided that the current codebase is already stable enough to be treated as the first real project baseline rather than only as the ending point of one optimization phase.

That means the project needs two things together:

1. a concrete baseline tag naming decision
2. a matching tag note that explains what this baseline means, what the project has already achieved, and how the current system should be introduced

## Outcome

- the first baseline tag naming proposal is now explicit as `baseline/2026-04-27-v0.1.0-tianshu`
- the project now has a reusable first-draft tag note for management communication and future baseline reference
- the baseline is framed as a project-level capability summary instead of as a narrow phase-optimization summary
