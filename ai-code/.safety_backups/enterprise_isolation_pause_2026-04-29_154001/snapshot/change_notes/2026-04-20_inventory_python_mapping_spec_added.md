# 2026-04-20 新增库存扩展字段与Python方法映射初稿

## 本轮目标

- 新增 `06_库存扩展字段与Python方法映射草稿.md`
- 将库存专题从 XML 草稿继续下沉到 Python 模型层映射草稿

## 修改文件

- `仓管模块设计/01_模块设计/03_库存管理/06_库存扩展字段与Python方法映射草稿.md`

## 修改原因

- 库存专题已经形成字段清单和 XML 草稿，但实现前仍缺少一份专门回答“这些字段和按钮方法应该挂在哪个模型文件”的模型层映射稿
- 若不先固定模块归属、字段类型和方法职责，后续实现时容易一边写 XML 一边重新猜模型设计

## 改动摘要

- 新增文档定位、范围边界、统一目标和章节总览
- 固定 `stock.quant / stock.location / stock.warehouse` 三类对象在 `logistics_wms_base` 与 `logistics_wms_inventory` 间的建议归属
- 固定三类对象的建议模型文件路径
- 补齐三类对象的扩展字段、计算字段和 Smart Button 方法映射表
- 固定 Smart Button 方法的统一职责边界和返回动作结构建议
- 固定计算字段、搜索字段、依赖关系和 `store=False` 优先策略建议
- 补齐实施顺序和模型层验证点

## 影响范围

- `仓管模块设计/01_模块设计/03_库存管理/`
- 后续库存专题的 `models/*.py` 实现、按钮方法回填和字段定义将以本稿为模型层基线

## 是否影响模型 / 视图 / 权限 / 数据兼容性

- 不直接影响真实模型、视图、权限或数据兼容性
- 本次为设计文档新增，后续若按文档落地，可能影响模型文件拆分、字段类型、计算字段实现方式和按钮方法命名

## 验证方法

- 检查文档是否已形成完整章节闭环
- 检查 XML 草稿里引用的字段和按钮方法是否都已在本稿中找到模型层落位建议
- 检查模块归属、字段命名和方法职责是否与数据库稿、字段清单、XML 草稿保持一致

## 风险点

- 当前字段和方法为设计级建议，真实实现时仍需结合现有模型字段、原生预留逻辑和追溯对象关系进一步锁定
- `stock.quant` 上的货主、仓库和冻结量实现后续仍可能因底层对象关系而细化
- 若后续动作目标页、服务端命名或模块拆分方式调整，本稿中的映射关系也需要同步回写

## 回滚建议

- 若后续决定重做库存模型层拆分方式，可删除本稿并回退到由 XML 草稿与字段清单分散承接的状态
- 回滚前建议先确认是否已有真实 Python 实现或开发任务基于本稿继续扩写

## 后续待办

- 视需要继续补库存扩展字段到真实 `models/*.py` 代码骨架草稿
- 视需要继续补 Smart Button 服务端方法返回值模板草稿
- 若模型层映射评审通过，可同步回填总导航和进度总结
