# Change Notes

## 本轮目标

- 产出 `logistics_base` 的正式 addon 设计稿

## 修改文件

- `ai-code/docs/architecture/logistics_base_addon_design.md`

## 修改原因

- 现有 `ai-code` 已具备框架和草图，但还缺少可直接指导首个自定义 addon 落地的正式设计文档

## 改动摘要

- 明确了 `logistics_base` 的职责边界
- 明确了与 `contacts`、`hr`、`stock` 的依赖关系
- 固定了扩展模型、建议字段、视图策略、权限策略和 manifest 草案
- 给出了实施顺序、验证建议和风险点

## 影响范围

- 仅影响 `ai-code` 文档体系
- 不影响现有业务代码

## 是否影响模型 / 视图 / 权限 / 数据兼容性

- 不直接影响实际模型
- 不直接影响实际视图
- 不直接影响实际权限
- 不直接影响数据兼容性

## 验证方法

- 检查设计稿是否完整覆盖模块目标、依赖、模型、视图、权限、验证、风险
- 检查内容是否与现有一期边界一致

## 风险点

- 该设计稿仍属于文档级设计，真实编码前还需根据实际样本数据做局部微调

## 回滚建议

- 若不再采用该设计方案，可直接删除本设计稿文件

## 后续待办

- 基于该设计稿开始创建 `custom_addons/logistics_base`
- 生成首版 manifest、models、views
- 完成首次升级与人工验证记录
