diff --git "a/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\256\236\347\216\260\347\274\272\345\217\243\346\270\205\345\215\225.md" "b/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\256\236\347\216\260\347\274\272\345\217\243\346\270\205\345\215\225.md"
deleted file mode 100644
index 65fb002fbd39..000000000000
--- "a/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\256\236\347\216\260\347\274\272\345\217\243\346\270\205\345\215\225.md"
+++ /dev/null
@@ -1,357 +0,0 @@
-# 2026-04-27 企业隔离 `P1` `customer_line` 主阅读入口实现缺口清单
-
-## 状态说明
-
-- 本文承接的是“`customer_line` 作为 `P1` 主阅读入口”的旧讨论前提。
-- 2026-04-27 已确认当前业务按“一运单一门店”强约束收口后，本文不再作为当前 `P1` 的现行实现目标清单。
-- 本文继续保留，作为历史讨论和未来如需恢复多门店节点阅读层时的参考材料。
-- 当前现行口径请优先阅读：
-  - `2026-04-27_企业隔离P1_一运单一门店口径调整说明.md`
-  - `企业隔离方案可行性与分期落地调整说明.md`
-
-## 1. 文档定位
-
-- 本文承接 `2026-04-27_企业隔离P1_customer_line主阅读入口必要性说明.md`。
-- 本文不再解释“为什么重要”，而是直接回答：
-  - 要让 `customer_line` 真正成为图片、留痕、异常的主阅读入口，还缺哪些具体实现点
-  - 这些实现点分别属于哪一层
-  - 哪些应优先做，哪些可以作为配套补强
-- 本文仍只覆盖企业隔离 `P1`，不扩展到 `P2 / P3` 平台设计。
-
-## 2. 当前现实起点
-
-### 2.1 已经有的部分
-
-- `customer_line` 对象、列表页、表单页、搜索页已经存在：
-  - `custom_addons/logistics_dispatch/views/logistics_dispatch_waybill_views.xml`
-  - `custom_addons/logistics_dispatch/models/logistics_dispatch_waybill_customer_line_v2.py`
-- `waybill` 页已有留痕、证据、异常摘要和聚合字段：
-  - `trace_count`
-  - `evidence_count`
-  - `open_exception_count`
-- `trace_event / evidence / exception` 三层正式对象已经存在：
-  - `logistics.trace.event`
-  - `logistics.trace.evidence`
-  - `logistics.trace.exception`
-
-### 2.2 还没完成的关键点
-
-当前 `customer_line` 虽然“存在”，但它还没有完成主阅读入口所需要的整条实现链：
-
-- 页面入口链
-- 查询取数链
-- 反读跳转链
-- 图片命中补链
-- 权限与导出审计链
-
-## 3. 缺口清单总览
-
-当前最关键的缺口建议拆成 8 项：
-
-1. `customer_line` 页缺少 trace / evidence / exception 摘要区
-2. `customer_line` 页缺少直达 trace / evidence / exception 的入口动作
-3. 图片 reader 仍按 `waybill_id` 取数，未形成 `customer_line` 视角 reader
-4. 留痕反读规则未正式落成 `customer_line -> trace_event` 查询桥
-5. 异常反读规则未正式落成 `customer_line -> exception` 查询桥
-6. 图片导入与图片包补链未形成 `customer_line` 命中闭环
-7. 导出与预览权限还没有按 `customer_line` 主阅读链细化
-8. 联调与验收还没有把 `customer_line` 入口作为独立通过条件
-
-下面逐项展开。
-
-## 4. 具体实现缺口
-
-### 4.1 缺口一：`customer_line` 页缺少三层摘要区
-
-#### 当前现实
-
-`customer_line` 表单页当前主要承载：
-
-- 客户信息
-- 汇总信息
-- 履约说明
-- 货物明细
-
-但没有稳定的：
-
-- 留痕摘要区
-- 证据摘要区
-- 异常摘要区
-
-#### 为什么这是缺口
-
-如果没有摘要区，`customer_line` 页即使有对象表单，也只是“信息页”，还不是“阅读入口页”。
-
-#### 建议补法
-
-在 `customer_line` 表单页首屏补最小摘要区：
-
-- 最近相关留痕类型
-- 最近相关留痕时间
-- 相关证据数
-- 相关异常数
-- 当前证据状态
-- 当前异常状态
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.2 缺口二：`customer_line` 页缺少三层直达入口
-
-#### 当前现实
-
-当前 `waybill` 页已有较成熟入口：
-
-- 运单侧留痕 smart button
-- 运单侧证据聚合
-- 运单侧异常聚合
-
-但 `customer_line` 页还没有对应的直达动作。
-
-#### 为什么这是缺口
-
-没有入口，就无法让用户形成“先看门店节点，再下钻事实材料”的稳定使用路径。
-
-#### 建议补法
-
-在 `customer_line` 表单页补三个入口：
-
-- 查看相关留痕
-- 查看相关证据
-- 查看相关异常
-
-要求：
-
-- 入口是阅读入口，不改变正式对象归属
-- 入口打开后的 domain 必须是“与当前 `customer_line` 相关”，不是整单全量
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.3 缺口三：图片 reader 仍按 `waybill_id` 取数
-
-#### 当前现实
-
-当前运单页前端 evidence reader 直接按 `waybill_id` 查询 `logistics.trace.evidence`。
-
-这意味着实现层默认仍是：
-
-- 运单页是主 reader
-- `customer_line` 只是运单内部结构
-
-#### 为什么这是缺口
-
-只要读取仍按运单聚合，`customer_line` 就很难真正成为图片主阅读入口。
-
-#### 建议补法
-
-至少补一层 `customer_line` 视角 reader：
-
-- 方案 A：新增 `customer_line` 页 evidence reader
-- 方案 B：保留 waybill reader，但新增按 `customer_line` 分组与切换
-
-无论哪种方案，都要满足：
-
-- 默认从 `customer_line` 进入时，先看当前节点相关图片
-- 运单页只承担汇总和总览，不再独占图片主阅读职责
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.4 缺口四：留痕反读桥未正式落成
-
-#### 当前现实
-
-当前正式留痕对象仍围绕：
-
-- `batch_id`
-- `waybill_id`
-
-这是正确的，但 `customer_line` 到 `trace_event` 的反读规则没有正式收口成实现约束。
-
-#### 为什么这是缺口
-
-如果没有反读桥，`customer_line` 的留痕入口就只能：
-
-- 打开整单 trace
-- 或靠人为理解去筛
-
-这不算真正的门店节点阅读入口。
-
-#### 建议补法
-
-首轮先不改正式对象挂点，只补反读规则：
-
-- 以 `waybill` 为基础范围
-- 再按事件类型和业务映射收口到当前 `customer_line` 相关事件
-
-首轮建议纳入门店节点摘要反读的事件：
-
-- `arrive_store`
-- `deliver_finish`
-- `signoff`
-- `exception_report`
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.5 缺口五：异常反读桥未正式落成
-
-#### 当前现实
-
-异常详情已经能反开：
-
-- `waybill`
-- `batch`
-- `trace_event`
-- `evidence`
-
-但 `customer_line` 页还没有形成“查看本节点相关异常”的稳定入口和 domain。
-
-#### 为什么这是缺口
-
-异常如果只能从运单或异常列表进，就会继续停留在“整单视角优先”，而不是“门店节点视角优先”。
-
-#### 建议补法
-
-补 `customer_line -> exception` 反读入口，首轮查询逻辑建议：
-
-- 先限当前 `waybill`
-- 再按 `trace_event` 相关店侧事件筛
-- 保留异常正式主挂点围绕 `waybill / batch / trace_event`
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.6 缺口六：图片命中与补链未形成 `customer_line` 闭环
-
-#### 当前现实
-
-目前已经有：
-
-- `customer_line_no`
-- `store_no`
-- `image_package` 枚举
-
-但还没有形成清晰落地的：
-
-- 图片包导入服务
-- 命中规则
-- 补链规则
-- 证据化规则
-
-#### 为什么这是缺口
-
-如果图片不能稳定命中 `customer_line`，那“图片围绕 `customer_line` 阅读”就只是口径，不是能力。
-
-#### 建议补法
-
-首轮命中规则固定为：
-
-1. `waybill_no + customer_line_no`
-2. 兜底 `waybill_no + store_no`
-
-补链顺序固定为：
-
-1. 先命中 `customer_line`
-2. 再关联到对应 `trace_event`
-3. 最后进入正式 `evidence`
-
-并且明确：
-
-- 不允许按门店名称、地址自由猜测命中
-- 不允许直接把图片主挂到 `order_line`
-
-#### 建议优先级
-
-`P1` 必补。
-
-### 4.7 缺口七：权限、导出、审计未按 `customer_line` 入口细化
-
-#### 当前现实
-
-当前权限还偏粗，更多停留在：
-
-- 普通用户
-- manager
-
-且导出与预览还没有完全按门店节点入口分层。
-
-#### 为什么这是缺口
-
-如果入口切到 `customer_line`，但权限和导出仍只有整单级理解，就会出现：
-
-- 门店节点可读但不可控
-- 只能整单导出，不能按节点导出
-- 阅读入口变了，审计口径没跟上
-
-#### 建议补法
-
-围绕 `customer_line` 主阅读链至少补清：
-
-- 谁能看节点图片
-- 谁能导出节点图片
-- 谁能查看节点相关异常
-- 谁能补传或补链
-- 节点级预览、下载、导出是否记审计
-
-#### 建议优先级
-
-`P1` 强建议，最好与入口一起补。
-
-### 4.8 缺口八：联调与验收还没把 `customer_line` 入口设为硬通过项
-
-#### 当前现实
-
-当前实施清单和验收清单已经开始强调 `customer_line`，但还需要进一步把它变成更明确的硬通过条件。
-
-#### 为什么这是缺口
-
-如果联调只验证：
-
-- 模块能装
-- 运单页能看
-- 异常能流转
-
-那么团队很容易在联调阶段默认“运单可读就算过”，从而把 `customer_line` 入口再次弱化。
-
-#### 建议补法
-
-在联调和验收中明确新增以下通过项：
-
-- `customer_line` 页可看到相关留痕摘要
-- `customer_line` 页可看到相关证据摘要
-- `customer_line` 页可看到相关异常摘要
-- `customer_line` 页可直达三层详情
-- 运单页不再是图片唯一主阅读入口
-
-#### 建议优先级
-
-`P1` 必补。
-
-## 5. 推荐实现顺序
-
-为了避免返工，建议按下面顺序推进：
-
-1. 先补 `customer_line` 页摘要区与直达入口
-2. 再补 `customer_line -> trace_event` 反读桥
-3. 再补 `customer_line` 视角 evidence reader
-4. 再补 `customer_line -> exception` 反读桥
-5. 再补 `image_package` 命中与补链闭环
-6. 最后补节点级权限、导出、审计与验收硬条件
-
-## 6. 当前最关键的边界提醒
-
-- 不把 `customer_line` 升格成正式 `trace_event` 主挂点
-- 不把正式 `evidence` 主挂点从 `trace_event` 改成 `customer_line`
-- 不把异常主挂点从 `waybill / batch / trace_event` 改成 `customer_line`
-- 不把“门店节点主阅读入口”误解成“正式对象主归属入口”
-
-## 7. 一句话收口
-
-`customer_line` 要真正成为企业隔离 `P1` 下的门店侧主阅读入口，不是只补一个页面按钮，而是要把摘要区、直达入口、按节点取数、反读桥、图片补链、权限导出和验收条件整条链一起补齐。
diff --git "a/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\277\205\350\246\201\346\200\247\350\257\264\346\230\216.md" "b/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\277\205\350\246\201\346\200\247\350\257\264\346\230\216.md"
deleted file mode 100644
index c3b689dcceac..000000000000
--- "a/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/2026-04-27_\344\274\201\344\270\232\351\232\224\347\246\273P1_customer_line\344\270\273\351\230\205\350\257\273\345\205\245\345\217\243\345\277\205\350\246\201\346\200\247\350\257\264\346\230\216.md"
+++ /dev/null
@@ -1,215 +0,0 @@
-# 2026-04-27 企业隔离 `P1` `customer_line` 主阅读入口必要性说明
-
-## 状态说明
-
-- 本文形成于“`customer_line` 计划作为 `P1` 主阅读入口”的讨论阶段。
-- 2026-04-27 已确认当前业务按“一运单一门店”强约束收口后，本文不再作为企业隔离 `P1` 的现行基线。
-- 本文保留为历史讨论留痕，便于回看当时为什么会围绕 `customer_line` 展开。
-- 当前现行口径请优先阅读：
-  - `2026-04-27_企业隔离P1_一运单一门店口径调整说明.md`
-  - `企业隔离方案可行性与分期落地调整说明.md`
-
-## 1. 文档定位
-
-- 本文用于沉淀企业隔离 `P1` 讨论中的三个关键判断。
-- 本文不是新方案稿，而是对当前方案收口逻辑的解释性说明。
-- 本文主要回答：
-  - 为什么当前企业隔离专题会先补 `trace / evidence / exception`
-  - 为什么 `customer_line` 还没有真正成为图片、留痕、异常的主阅读入口
-  - 为什么如果不围绕 `customer_line` 建立主阅读入口，就会影响当前定义下的 `P1`
-
-## 2. 为什么当前专题先补 `trace / evidence / exception`
-
-这并不代表企业隔离专题跑偏成了留痕专题，而是因为当前企业隔离 `P1` 的真实目标不是平台控制面，而是：
-
-- 数据库级企业隔离底盘成立
-- 每个租户库能运行同一套现有物流模块
-- 租户化后不打坏当前物流主线、页面主阅读链和图片阅读链
-
-在这个前提下，当前最稳定的主链其实已经是：
-
-- `wave -> batch -> waybill -> customer_line -> order_line -> goods_line`
-
-真正最容易在租户化过程中发生口径漂移的，是下面三层：
-
-- `trace`
-- `evidence`
-- `exception`
-
-原因是执行主链对象已经基本稳定，但以下问题如果不先锁住，租户化以后仍然会乱：
-
-- `customer_line` 到底是不是门店节点主阅读入口
-- 图片业务阅读到底围绕谁
-- 正式证据对象到底挂谁
-- 异常到底围绕谁流转
-- 租户内查看、提交、处理、导出边界怎么切
-
-所以当前补出的这些文档，本质上是在处理：
-
-**企业隔离 `P1` 下，最容易因为租户化而失控的业务承接层设计。**
-
-## 3. 为什么 `customer_line` 还没有真正成为主阅读入口
-
-当前 `customer_line` 已经在对象和页面上存在，但它还没有把“主阅读入口”真正落成闭环。
-
-更准确地说，`customer_line` 目前只完成了：
-
-- 对象存在
-- 页面存在
-- 业务意义存在
-
-但还没有完整完成下面四层。
-
-### 3.1 页面入口层还主要长在 `waybill`
-
-当前 `waybill` 页已经有留痕、证据、异常的聚合字段和摘要入口，且 `trace_core` 已给运单表单增加留痕 smart button。
-
-但 `customer_line` 页当前主要还是：
-
-- 客户信息
-- 汇总信息
-- 货物明细
-
-尚未形成稳定的：
-
-- 留痕摘要区
-- 证据摘要区
-- 异常摘要区
-- 直达 trace / evidence / exception 的入口
-
-所以它现在还是“有节点页”，还不是“主阅读入口页”。
-
-### 3.2 查询与服务层还主要按 `waybill` 拉数据
-
-当前运单页的证据读取逻辑仍然是直接按 `waybill_id` 查询证据列表。
-
-这意味着：
-
-- 实现层的图片阅读链仍以运单聚合视角为主
-- `customer_line` 还没有形成自己的 reader 或自己的查询域
-- 门店节点页还不能稳定承接“按门店节点读图、读留痕、读异常”
-
-### 3.3 正式挂点没问题，但 `customer_line` 的反读桥还没补完整
-
-当前正式对象挂点本身是正确的：
-
-- `trace_event` 继续围绕 `batch / waybill`
-- `evidence` 继续围绕 `trace_event`
-- `exception` 继续围绕 `waybill / batch / trace_event`
-
-问题不在正式挂点，而在于：
-
-**从 `customer_line` 到这些正式对象的反读桥还没有真正补完整。**
-
-也就是还缺：
-
-- `customer_line` 如何找到相关 trace
-- `customer_line` 如何读取本节点相关证据
-- `customer_line` 如何看到本节点相关异常摘要
-
-### 3.4 图片命中与补链逻辑还没正式落稳
-
-当前口径已经收口到：
-
-- 图片业务阅读优先围绕 `customer_line`
-- 图片命中优先按 `waybill_no + customer_line_no`
-- 兜底按 `waybill_no + store_no`
-
-但实际实现层，`image_package` 虽然已经进入任务对象枚举，图片包导入、命中、补链、证据化流程还没有完整落到模块里。
-
-没有这层，`customer_line` 就只能停留在“应该成为入口”，而不是“系统已经按它来读”。
-
-## 4. 为什么不围绕 `customer_line` 建主阅读入口，会影响当前定义下的 `P1`
-
-这里需要先说清楚一句话：
-
-**不是说没有 `customer_line` 就完全不能做 `P1`，而是说如果不把它立成主阅读入口，做出来的就不是当前定义下合格的 `P1`。**
-
-### 4.1 `P1` 的要求不只是“分库成功”和“模块能装”
-
-当前企业隔离 `P1` 的定义不是只有：
-
-- 分库成功
-- 模块能装
-- 页面能打开
-
-还包括：
-
-- 现有物流主线不变形
-- 当前四期前端阅读链不变形
-- 图片、留痕、异常阅读口径不倒退
-
-既然当前有效基线已经收口成：
-
-- `waybill -> customer_line -> order_line`
-- `waybill -> customer_line -> 图片预览 / 留痕 / 证据`
-
-那 `P1` 如果不围绕 `customer_line` 建主阅读入口，就已经偏离当前 `P1` 定义。
-
-### 4.2 不主动立 `customer_line`，系统会自然倒回运单独占阅读
-
-如果不补 `customer_line` 入口，最省事的实现路径一定会变成：
-
-- 留痕继续只在 `waybill` 看
-- 图片继续只在 `waybill` 按整单聚合看
-- 异常继续只从 `waybill` 或异常列表进
-
-这样当然“能用”，但它会把门店节点层重新压扁。
-
-结果就是：
-
-- 店侧事实阅读会越来越糊
-- 图片只剩整单汇总视角
-- 哪个门店节点缺证据、哪个节点有异常，很难稳定阅读
-
-这会直接和四期前端已经锁下来的对象分层冲突。
-
-### 4.3 `customer_line` 不是装饰层，而是门店现场事实承接层
-
-当前对象链之所以恢复出 `customer_line`，不是为了页面好看，而是因为业务上确实需要这一层：
-
-- 一个 `waybill` 下可能有多个门店节点
-- 每个门店节点的到店、签收、图片、异常上下文不完全一样
-- 如果不在 `customer_line` 这一层承接，很多现场事实只能被粗暴并回运单级聚合
-
-所以问题不是“要不要多一个入口”，而是：
-
-**没有 `customer_line` 主阅读层，店侧事实就会被运单级汇总再次吞掉。**
-
-### 4.4 这不是可轻松后补的点，而是会牵动后续返工
-
-如果 `P1` 先不立 `customer_line` 主阅读入口，后面一旦要补，通常会连带牵动：
-
-- 页面入口
-- 查询 domain
-- 证据分组方式
-- 异常摘要口径
-- 导出范围
-- 审计口径
-
-所以这不是“先跳过，后面轻轻补一下”的问题，而更像是：
-
-**如果本轮不锁，后续 `evidence` 和 `exception` 很可能一起返工。**
-
-## 5. 当前最准确的结论
-
-### 5.1 关于当前专题为什么偏向 `trace / evidence / exception`
-
-- 当前补这些文档，不是因为企业隔离专题跑偏
-- 而是因为企业隔离 `P1` 下最容易失控的正是这三层业务承接
-
-### 5.2 关于 `customer_line` 为什么还没真正站住主阅读入口
-
-- 方向已经锁了
-- 对象和页面已经有了
-- 但入口、查询、反读桥、补链、权限与导出闭环还没跟齐
-
-### 5.3 关于“不围绕 `customer_line` 是否还能做 `P1`”
-
-- 从纯技术角度讲，不围绕 `customer_line`，也能做出一个“能跑”的租户化版本
-- 但从当前已经锁定的 `P1` 目标讲，那不算合格的 `P1`
-- 因为它会违背当前前端基线，也会把门店节点阅读链做回旧口径
-
-## 6. 一句话收口
-
-当前企业隔离 `P1` 的关键，不只是把系统分成多个租户库，而是要在租户化后继续保住当前已经稳定下来的对象分层与阅读链；而这件事能不能成立，`customer_line` 是否真正成为门店侧主阅读入口，是最关键的判定点之一。
diff --git "a/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/\345\237\272\344\272\216Odoo\347\232\204\344\274\201\344\270\232\351\232\224\347\246\273\345\212\237\350\203\275\350\256\276\350\256\241.docx" "b/ai-code/\344\270\223\351\242\230\350\256\276\350\256\241/\344\274\201\344\270\232\351\232\224\347\246\273\350\256\276\350\256\241/00_\345\257\274\350\210\252\344\270\216\346\200\273\347\272\262/\345\237\272\344\272\216Odoo\347\232\204\344\274\201\344\270\232\351\232\224\347\246\273\345\212\237\350\203\275\350\256\276\350\256\241.docx"
deleted file mode 100644
index d913b07583ea34946a78b74490ed7b113805a5d6..0000000000000000000000000000000000000000
