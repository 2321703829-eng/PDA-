# 2026-04-17 二期分支模块与模型结构说明文档补充

## 本次补充内容

新增文档：

- `ai-code/前端设计/二期前端优化设计/05_提交包/2026-04-17_二期分支模块与模型结构说明.md`

## 文档目的

用于对外统一说明二期整体修改的三个关键信息：

1. 二期代码当前同步在哪个分支
2. 二期到底改了哪些模块
3. 二期是否新增了数据库字段或模型结构，以及应如何区分

## 本次文档覆盖范围

不是只覆盖本次 `import_center` 资产修复，而是覆盖：

- 上一次整批同步到 `feat-image-4a` 的二期优化内容
- 本次新增的导入中心前端资产修复内容

## 文档特点

文档明确区分了：

- 真正新增的模型/表
- 明确扩展的业务模型结构
- 用于导入导出的 `compute/inverse` 业务别名字段
- 前端承载层模块与持久化模型层的边界
