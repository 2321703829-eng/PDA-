# 2026-04-20 状态流程专题补数据库字段总表与权限矩阵逐项映射

## 本次变更

- 在 `状态与流程统一规范草稿` 中新增：
  - `17. 与数据库字段总表之间的逐项映射`
  - `18. 与权限矩阵之间的逐项映射`

## 本次补充重点

- 把主状态、异常补充状态、执行记录状态分别对齐到数据库字段方向
- 把四类主单与数据库稿中的 `state / exception_state / 执行记录 state` 对齐
- 把状态层继续对齐到权限专题中的：
  - 单据级动作矩阵
  - 页面区块级按钮显隐矩阵
  - 四层联动总表

## 当前结果

- 状态总规范不再只是原则稿，而是开始承担状态 -> 字段 -> 权限的中间对齐层
- 后续继续写对象编码、执行页按钮校对、数据库字段编号映射时，可以直接从这两节继续往下扩
