# 2026-04-18 权限与角色规范补充模块文件树总索引

## 本次变更

补充 `仓管模块设计/01_模块设计/08_设置与规则/00_权限与角色规范草稿.md`：

- 新增 `21. 权限规则到具体模块文件树的总索引`
- 将原 `v0.1 收口与后续补充方向` 顺延为 `22`
- 同步补齐结尾收口中的已固定内容与后续方向

## 本次补充的重点

### 1. 把权限专题继续推进到模块文件树层

本次在前面已经补过的：

- XML / Python / JS 对象映射
- 角色 / 状态 / 按钮命名落位

基础上，进一步补了：

- `logistics_wms_base`
- `logistics_wms_inbound`
- `logistics_wms_inventory`
- `logistics_wms_outbound`
- `logistics_wms_web`

这几个模块各自更适合承接哪些权限相关对象。

### 2. 固定了当前推荐的模块分工方向

本次明确：

- `wms_base` 先收基础 group、基础 record rule 与共性安全边界
- 入库 / 出库 / 库内作业单据权限各自跟着业务模块走
- 执行页交互相关的前端权限统一落在 `logistics_wms_web`

这样后面开始真正拆目录和文件时，就不会再反复争论“安全应该写哪、前端应该写哪”。

### 3. 补了一组目录级索引表

本次分别给出了：

- 安全目录总索引
- XML 文件树索引
- Python 文件树索引
- JS / OWL 文件树索引

让这份权限专题继续从“规则和对象”推进到了“模块与目录”层。

## 当前意义

这次补充后，这份权限专题已经不只是在讲权限该怎么理解，而是开始回答这些规则最终更适合沉到哪些模块目录下。  
后续如果继续补“更细的前端权限辅助 service / hook 设计口径”，这份权限稿就会继续向真正的开发分工清单推进。
