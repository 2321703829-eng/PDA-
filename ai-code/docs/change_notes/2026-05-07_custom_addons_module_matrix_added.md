# custom_addons 第二轮模块治理

## 本轮目标

为 `custom_addons/` 补一份模块主文档清单与职责矩阵，统一列出各模块的模型主对象、API 入口、前端承接和归属专题。

## 本轮变更

1. 新增 `custom_addons/模块职责矩阵.md`，包含：
   - 现行模块矩阵
   - 模块主文档清单
   - API 与前端承接说明
   - 归属专题说明
2. 更新 `custom_addons/README.md`，把矩阵文件加入根入口和默认读法。

## 本轮结论

- `custom_addons/` 现在不只是有模块入口，还有一份跨模块的职责总表。
- 需要判断“对象归谁、接口放哪层、前端由谁承接、主要归属哪个专题”时，已经有统一入口可查。
