# 2026-04-13 仓储管理前端接入草案完成

## 本次变更

新增文档：

- `前端相关设计/仓储管理前端接入设计草案.md`

## 变更目的

根据 `前端设计新工作计划安排.md` 第四阶段安排，补齐 `仓储管理` 模块的正式前端接入设计，明确：

- 哪些仓储页面直接复用 Odoo 原生入口
- 哪些仓储页面需要增加 Smart Button 和追溯入口
- 仓储页面应暴露哪些追溯关联摘要字段
- 仓储页面如何跳转到批次、运单、留痕、异常主链页面

## 本次收口结论

1. `仓储管理` 继续定位为业务接入层，不承担主阅读链职责
2. 当前以复用 Odoo 原生 `stock` 页面为主，不重做仓储主页面
3. `stock.picking` 是仓储侧最优先增强的前端入口
4. 仓储接入优先围绕 `批次`、`运单`、`留痕`、`异常` 建立反查关系
5. 一期设计不依赖二期调度资源对象作为必备前提

## 影响范围

- `前端相关设计/` 文档体系补齐了第四阶段的首份模块文档
- 为后续 `业务单据前端接入设计草案.md` 与 `Smart Button设计清单.md` 提供统一口径

## 验证

本次为文档新增，无代码变更，无模块升级，无运行验证。
