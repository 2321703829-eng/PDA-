# 2026-04-22 phase4 database design add table build implementation draft

## 本次调整

将《四期数据库底表设计稿 v1（第二次修正版）》继续推进为可建表实施稿，补齐了建表时必须统一的实施口径。

## 新增内容

- 新增字段类型、长度、默认值统一口径
- 新增所有 `Many2one` 的 `ondelete` 矩阵
- 新增按表建表实施清单
  - 字段类型/长度重点
  - 默认值重点
  - 索引重点
  - 删除策略/备注
- 新增编号与 sequence 规则
  - 明确哪些表使用独立业务编号
  - 明确历史表和日志表是否使用独立编号
- 明确金额、数量、重量、体积、时间字段的统一数据类型精度

## 目的

让设计稿从“可讨论设计”升级为“可直接指导 Odoo model / SQL DDL 落地”的实施稿。
