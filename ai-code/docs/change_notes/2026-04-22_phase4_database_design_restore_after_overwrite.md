# 2026-04-22 phase4 database design restore after overwrite

## 本次调整

目标设计稿在被旧版本覆盖后，重新补回了当日已确认的数据库设计变更，并在文档正文开头增加了显眼的修订记录。

## 恢复内容

- 恢复 `logistics_customer_profile` 与门店配送画像扩展字段
- 恢复司机/车辆三层设计：
  - 基本信息层
  - 当前调度状态层
  - 历史业务记录层
- 恢复“司机与车辆非固定强绑定”的架构口径
- 恢复约束、索引、状态枚举、写入边界、导入/操作日志同步规则
- 恢复 `司机列表.xlsx` 与 `车辆列表.xlsx` 对应字段的完整覆盖口径

## 可见性补充

- 在目标文档开头新增“本次追加修订记录”，便于后续复核是否再次被覆盖
