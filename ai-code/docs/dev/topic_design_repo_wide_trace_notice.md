# 专题设计目录归并全仓留痕说明

## 1. 说明目的

本说明用于给仓库内所有读者和后续 AI 协作者一个统一口径：

- 专题设计目录已经完成物理归并
- 旧根目录仍可能出现在历史文档中
- 这些旧路径并不代表当前真实入口

## 2. 当前真实入口

当前专题设计唯一真实根目录为：

- `专题设计/前端设计/`
- `专题设计/仓管设计/`
- `专题设计/企业隔离设计/`
- `专题设计/高并发优化设计/`

统一根入口为：

- `专题设计/README.md`

仓库级导航入口为：

- `专题包导航总表.md`
- `README.md`
- `docs/README.md`

## 3. 旧路径映射

| 历史目录名 | 当前目录 |
| --- | --- |
| `前端设计/` | `专题设计/前端设计/` |
| `仓管模块设计/` | `专题设计/仓管设计/` |
| `企业分离功能文件夹/` | `专题设计/企业隔离设计/` |
| `高并发承载能力优化设计/` | `专题设计/高并发优化设计/` |

## 4. 如何理解旧路径

如果在仓库里看到旧路径，请按以下规则理解：

1. 出现在 `docs/change_notes/` 中：
   视为历史留痕，不代表当前入口错误。
2. 出现在已执行方案或迁移映射表中：
   视为迁移前后关系说明，不需要回写清洗。
3. 出现在现行入口或现行专题正文中：
   视为应继续治理的残留项。

## 5. 当前历史策略

- 旧根目录已经移除。
- 旧根目录名只作为历史路径、迁移映射或执行留痕保留在文档中。
- 任何新增入口、正文或导航都应直接使用 `专题设计/` 下的新路径。

## 6. 后续默认口径

以后在新增文档、补链接、写说明、做导航时：

- 统一使用 `专题设计/` 下的新路径
- 不再使用旧根目录名作为现行展示名
- 只有在解释历史迁移过程时，才保留旧目录名
