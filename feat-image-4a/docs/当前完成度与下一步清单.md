# feat-image-4a 当前完成度与下一步清单

## 一、当前已完成内容

### 1. 模块开发与接入
- 已完成 `logistics_trace_core` 与 `logistics_trace_evidence` 两个标准 Odoo addon 的开发。
- 两个模块当前均位于 `feat-image-4a/addons/` 下，符合本分支独立协作的目录要求。
- 已在服务器测试环境中通过 `extra-addons` 挂载方式接入 Odoo 容器。

### 2. 服务器侧真实部署验证
- 已确认 Odoo 测试环境运行于 Docker 容器中。
- 已将模块复制到宿主机目录：
  - `/opt/odoo-lite-test/extra-addons/logistics_trace_core`
  - `/opt/odoo-lite-test/extra-addons/logistics_trace_evidence`
- 已重启 `odoo-lite-test-web` 容器，且容器日志未出现明显模块加载错误。

### 3. 模块安装验证
- 已在 Odoo 后台成功识别并安装：
  - `Logistics Trace Core`
  - `Logistics Trace Evidence`
- 已确认模块安装后可进入对应菜单与设置项。

### 4. 参数配置验证
- 已配置系统参数：
  - `logistics_trace_evidence.image_public_base_url = http://8.166.131.218`
  - `logistics_trace_evidence.image_storage_root = /var/lib/odoo/logistics_trace_images`
- 已确认设置页中 `Logistics Trace Evidence` 配置区可正常显示。

### 5. 业务主链路验证
- 已成功创建一条运单级留痕事件：
  - `Biz Type = Waybill`
  - `Waybill No = WB20260413001`
- 已成功创建一条图片类证据对象：
  - `Evidence Type = Image`
- 已通过 Odoo 页面完成真实图片上传。
- 已在证据详情页看到真实生成的数据：
  - `Image Count = 1`
  - `Storage Provider = Local Storage`
  - `Storage Status = active`
  - `Mime Type = image/jpeg`
  - `image_access_key` 已生成

## 二、当前可以认定为已验证通过的点

- 模块可以被 Odoo 正常扫描与安装。
- 图片功能不是 mock，已经发生真实上传行为。
- 证据对象已成功挂接到运单级留痕事件。
- 图片存储逻辑走的是服务器侧存储方案，而不是 Odoo 默认附件图片展示思路。
- 当前这条线已经具备“留痕 -> 证据 -> 图片元数据”的最小闭环。

## 三、当前还需要补的验证项

### 1. 服务器真实落盘确认
需要在服务器执行：

```bash
find /opt/odoo-lite-test/odoo-data/logistics_trace_images -type f | sort
```

目标：
- 确认本次上传的图片已经真实写入宿主机目录。

### 2. 图片预览与下载验证
需要在 Odoo 页面继续验证：
- 点击 `Preview`
- 点击下载入口

目标：
- 确认预览的是真实图片。
- 确认下载得到的是真实文件。

### 3. 正常留痕场景补测
当前已走通的截图记录里，留痕被误操作为：
- `Trace Type = Exception`
- `State = Done`

这不影响图片功能本身验证，但建议补测一条“正常到达留痕”：
- `Biz Type = Waybill`
- `Trace Type = Arrive`
- 不点击 `Mark Exception`
- 直接保存

目标：
- 同时保留正常留痕与异常留痕两种样例。

### 4. 批次级场景补测
当前已验证的是运单级图片挂接。

还需补测：
- `Biz Type = Batch`
- `Batch No` 挂接图片

目标：
- 验证批次级图片场景与设计一致。

### 5. 主体混填校验补测
还需补测两条错误输入：
- `Biz Type = Waybill` 但同时填 `Batch No`
- `Biz Type = Batch` 但同时填 `Waybill No`

目标：
- 确认系统正确阻止保存。

## 四、下一步建议顺序

### P0：先收尾当前真实验收
1. 在服务器确认图片真实落盘。
2. 在 Odoo 页面确认预览与下载可用。
3. 保存截图与终端输出，形成验收证据。

### P1：补齐业务验证
1. 新建一条正常运单留痕并上传图片。
2. 补测批次级图片上传。
3. 补测批次/运单混填错误校验。

### P2：面向协作合库整理
1. 补一版“实际通过截图与命令记录”。
2. 将本模块已实现边界、已验证边界、未验证边界写清楚。
3. 方便后续与其他 `feat-*` 模块一起总装验收。

## 五、当前结论

`feat-image-4a` 目前已经不是“仅完成代码开发”的状态，而是已经在真实 Odoo 测试环境里跑通了最关键的一段：

- 安装模块
- 配置参数
- 创建运单级留痕
- 创建图片证据
- 上传真实图片

当前离“本模块阶段性验收通过”只差最后几项收尾验证：
- 服务器真实落盘确认
- 图片预览与下载确认
- 正常留痕与批次级补测
