# feat-main-4b 当前完成度与下一步清单

## 一、当前已完成

- 已创建独立功能线目录 `feat-main-4b`
- 已放入主链补全第一批模块：
  - `logistics_base`
  - `logistics_dispatch`
- 已建立独立文档目录，和 `feat-image-4a` 分线协作

## 二、当前模块范围

`feat-main-4b` 当前承接：

- 基础主数据扩展
- 调度执行主对象
- `wave / batch / waybill / waybill_order_line` 主链补全

`feat-main-4b` 当前不承接：

- 图片上传实现
- 证据存储实现
- 小程序图片接口实现

这些仍由 `feat-image-4a` 负责。

## 三、下一步

1. 检查 `logistics_base`、`logistics_dispatch` 与当前服务器环境的依赖兼容性
2. 对照仓库规范，收口主对象命名、状态机和编号规则
3. 在本地 Odoo 环境安装验证这两个模块
4. 评估与当前 `logistics_trace_core`、`logistics_trace_evidence` 的衔接方式
5. 确认是否进入服务器部署与主库安装

